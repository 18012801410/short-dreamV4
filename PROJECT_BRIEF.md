# 项目简报（Project Brief）—— short-dreamV4

> 状态：待批准
> 日期：2026-09-12
> 流程：头脑风暴工作流产出（用户已逐题确认意图与设计）

## 产品

**short-dreamV4**：个人自用的 AI 短视频生产平台。用户输入一个想法，平台沿"剧本 → 资产 → 分镜 → 视频 → 成片"流水线自动生成中间工件，每个阶段可审阅、编辑、重新生成，确认后推进。视频生成基于 **MiniMax H3 多参考生视频**（自带音频/台词），两项核心自动化：

1. **分镜引用资产图自动解析**：分镜段声明引用的角色/场景/道具资产，提交视频任务时自动从资产库解析出已批准的参考图。
2. **同场景尾帧接首帧**：同一场景内相邻视频段，自动用 ffmpeg 提取上一段视频尾帧，作为下一段 H3 请求的首帧参考（官方契约已核实 `first_frame` 与多参考互斥：有资产参考图时尾帧作为第一张参考图提交并对齐 0.00 秒，无参考图时才作 `first_frame`），保证跨镜头连续性。

## 问题

个人创作者从一句话想法产出带声音的 AI 短剧成片，需要在多个工具间手工搬运（写剧本 → 生设定图 → 写提示词 → 生视频 → 剪辑拼接），效率低；且角色/场景跨镜头一致性差，尾帧与首帧断裂导致画面跳变。

## 目标用户

仅项目所有者本人（个人自用工作台）。无账号体系、无多租户、无计费。

## 核心用户流程

1. 创建项目：输入想法 + 参数（题材/风格/目标时长/场景数）。
2. **剧本阶段**：LLM 生成剧本（版本化）→ 用户编辑/重生成 → 确认。
3. **资产阶段**：从剧本抽取角色/场景/道具 → 适配器生成设定图（可上传替换）→ 逐图批准锁定。
4. **分镜阶段**：LLM 生成分镜（场景 → 段（一次视频生成）→ 镜头），每段自动产出 H3 提示词并声明资产引用 → 用户编辑/重生成 → 确认。
5. **视频阶段**：Worker 逐段提交 H3 任务（自动解析参考图；continuity 开启的段自动取前段尾帧为首帧）→ 段级预览/重生成。
6. **合成阶段**：ffmpeg 拼接全部段（含音轨）→ 输出成片 mp4。
7. 全程在右侧任务中心看到进度/失败/重试/日志；支持"一键连跑"自动推进到下一个确认门。

## MVP 范围

### 必须有

- 项目创建与管理（想法 + 题材/风格/目标时长/场景数）
- 剧本生成、编辑、版本保留、确认门
- 资产抽取（角色/场景/道具）、资产图生成（Provider 适配器）、上传替换、批准锁定
- 分镜生成（场景→段→镜头结构，每段自动产出 H3 提示词 + 资产引用声明）、编辑、版本、确认门
- 参考图自动解析绑定（缺图阻断提示，不静默降级）
- MiniMax H3 视频生成：多参考图 + 首帧 + 自带音频；段级重生成；已成功段重跑不重复计费
- 尾帧→首帧连续性（段级 continuity 开关，同场景默认开；支持手动上传首帧覆盖）
- ffmpeg 尾帧提取与成片合成（含音轨）
- 任务系统：jobs 表持久化、Worker 轮询、重启可恢复、失败重试、进度展示
- 项目状态机与分段确认 + 一键连跑到下一确认门
- 设置页：供应商 API Key / 模型选择 / ffmpeg 路径 / 并发数

### 应该有

- 成本透明：任务级调用日志（模型、次数），便于估算花费
- 首段试跑（单段先行验证风格，再批量跑剩余段）
- 资产图片多视图/变体管理（同一角色多套设定图）

### 范围外（MVP 非目标）

- 多用户/账号/计费/配额
- BGM/配乐生成与混音（依赖 H3 自带音频，不做额外音轨）
- 独立对口型控制（用 H3 原生能力，不单独开发）
- 复杂时间线剪辑器（仅做顺序拼接）
- 云部署/对象存储/横向扩展
- 移动端适配
- 手绘分镜画板 / storyboard 图像

## 约束

- 运行环境：Windows 10/11 个人电脑；需自装 ffmpeg（平台启动时检测并提示）
- 技术栈（已拍板）：前端 React SPA；后端 Python FastAPI 单体；存储 SQLite + 本地文件目录；独立 Worker 进程轮询数据库任务表
- 外部依赖：RunningHub 工作流 API（视频 = MiniMax H3 导演台工作流；图片 = QWEN 文生图工作流）、LLM API（文本）；全部经 Provider 适配器接入，可配置切换
- 视频/图片生成均为付费 API：默认段级串行（并发数可配置），确认门 + 段级重生成控制废片成本

## 决策

| 决策 | 选择 | 原因 |
|---|---|---|
| 平台定位 | 个人自用工作台 | 最快跑通生产线，不做账号/多租户/计费，架构留扩展点 |
| 流程形态 | 分段确认式（每阶段审阅→确认→推进；支持一键连跑） | 视频生成付费且质量需人工把关；一处跑偏全链报废代价高 |
| 成片边界 | 带声音的完整成片，音频由 MiniMax H3 自带 | 无需单独接 TTS；成片 = H3 片段（含音轨）ffmpeg 拼接 |
| 生图来源 | Provider 适配器模式，MVP 先接一家 | 不被单一供应商锁死；后续可多家并存 |
| 生图默认实现 | RunningHub QWEN 文生图工作流（2026-09-12 用户拍板，原 MiniMax image-01 方案作废） | 用户已有 RunningHub 账号与工作流；质量不满意可配置切换 |
| LLM 接入 | Provider 适配器 + 可配置 `[AI-DECIDED]` 默认走环境变量（建议 GLM） | 剧本/分镜质量优先，保留切换自由 |
| 技术栈 | React + FastAPI（Python 后端） | Python 视频/图像处理生态强，未来接 ComfyUI/SD 更顺 |
| 任务架构 | FastAPI 单体 + SQLite 任务表 + 独立 Worker 进程 | 无外部中间件，Windows 一条命令可跑；任务持久化保证数分钟级视频任务重启不丢 |
| UI 形态 | 桌面优先三栏工作台 `[AI-DECIDED]` | 流水线导航 / 阶段工作区 / 任务中心，信息密度适合生产工具 |

**被否方案**：
- *Celery + Redis + PostgreSQL + MinIO 全套*——任务系统生产级，但 4 个中间件对单人 Windows 自用明显过重。
- *进程内 asyncio 后台任务*——代码最少，但重启丢任务、无法断点续跑，对数分钟级视频任务不可靠。
- *全自动一键到底*——省事但质量不可控，付费 API 重跑成本高。

## 成功标准

- 输入一个想法，走完全部确认门后，产出一条 ≥60 秒、多场景、跨镜头角色/场景外观一致、带 H3 原生音频的成片 mp4。
- 同场景相邻段的画面衔接无跳变（尾帧→首帧生效）。
- 每个阶段均可编辑并重新生成，重生成不影响已确认的其他阶段。
- Worker/服务重启后，进行中的任务能恢复或明确标记失败可重试，不出现永久卡死的任务。
- 全流程不需要离开平台去第三方工具手工搬运文件。

## 待定问题（Open Questions）

1. **视频/图片通道契约**：2026-09-12 用户拍板改走 RunningHub 工作流 API（H3 导演台 + QWEN 文生图），契约已核实并落入 AI_SPEC 附录 B；节点参数与端到端实测由 TASK-001 spike 完成。官方 V2 直连契约保留为附录 A 备选。
2. 尾帧质量风险：前段视频结尾若有运动模糊，作首帧效果差 → 已保留手动上传首帧覆盖作后门；是否需要"尾帧自动多帧择优"留待使用反馈。
3. LLM 默认模型（建议 GLM）与图片默认实现（建议 MiniMax 系）为 `[AI-DECIDED]` 代决，用户可随时在设置页更换。
4. 单段视频目标时长与整片目标时长的映射关系（H3 单次生成档位待 API 验证后定）。
