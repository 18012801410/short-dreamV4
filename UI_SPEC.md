# UI/交互规格（UI / Interaction Specification）—— short-dreamV4

## 页面

### 页面：项目列表（/）
用途：管理项目、查看流水线进度、继续工作。
入口：应用首页。出口：工作台 /project/:id、系列页 /series/:sid。
内容（TASK-047 用户拍板：**每部剧聚合为一张卡，单集不单列**）：
- 「系列剧」区：每部系列一张卡（剧名、系列状态、想法摘要、已建档 X/N 集 · 每集约 N 秒、
  「查看分集」按钮 → /series/{sid}）；系列的单集项目不出现在首页列表；
- 「项目」区：仅独立项目（series_id 为空），卡片含标题/状态/想法摘要/段进度/继续/归档；
- 创建表单支持 目标总时长（秒）与场景数（TASK-048 开放，此前写死 60s/3 场）。

### 页面：系列短剧（/series，TASK-047）
用途：从一个想法到一部分集剧：建系列 → 全剧大纲评审 → 批量生成分集剧本 → 逐集进工作台。
入口：首页头部「系列短剧」。出口：工作台 /project/:id（每集是普通项目）。
布局：左列系列列表（剧名/状态/集数×单集秒数/已建档数）+ 新建系列表单（想法、题材可空、
基调、集数=12、每集秒数=90、单集场景数=4）；右列选中系列详情：
- 大纲头卡：剧名/主线/题材标签/人物 chips/大爆点集；状态按钮按状态机出现——
  生成大纲（created/ready，ready 重新生成需确认覆盖）→ 确认大纲 + 编辑大纲（ready）→
  生成分集剧本（approved，仅对未建档集）；
- 质量门横幅：outline.warnings 逐条红字展示（硬门未过确认按钮会 422 给出 issues）；
- 编辑态：逐集卡片表单（集标题/开场钩子/梗概/爽点/集尾卡点），保存即重跑质量门；
- 分集列表：每集「第 N 集 · 标题」+ 钩子/爽点/卡点摘要 + 项目状态徽标 +
  「打开工作台」；SCRIPT_APPROVED 且非第 1 集的行显示「继承第1集资产」
  （零成本复制资产卡，跨集形象一致）。

### 页面：工作台（/project/:id）
用途：单项目全流程操作，核心页面。
入口：项目列表。出口：返回列表。
骨架见下方线框。

### 页面：设置（/settings）
用途：供应商/模型/key/并发/ffmpeg 配置与连通性测试。
入口：全局侧栏底部。出口：返回。

## 布局

### 线框（工作台）

```
+----------------------------------------------------------------------+
| 顶栏: ← 项目列表 | 标题·状态徽标           [一键连跑] [设置]          |
+----------+-----------------------------------------------+-----------+
| 流水线   | 阶段工作区（按当前阶段切换）                    | 任务中心   |
| ① 剧本 ✓ |  [剧本|资产|分镜|关键帧|视频|成片] ← Tab 可回看 | ── 进行中  |
| ② 资产 ● | +-------------------------------------------+ | · video   |
| ③ 分镜 ○ | |  剧本: 只读预览+编辑(draft) / 版本切换      | |   S01G02  |
| ④ 关键帧○| |  资产: 资产卡片墙(图+锚点+批准勾选+上传)    | | ▓▓▓░ 62%  |
| ⑤ 视频 ○ | |  分镜: 段表格(时长/镜头/引用/提示词编辑)    | | · compose |
| ⑥ 成片 ○ | |  关键帧: 段行(锚点描述/帧图/批准/上传/重抽) | | ── 失败   |
|          | |  视频: 段视频墙(预览/重生成/首段试跑)       | | [重试][日志]|
| 阶段门   | |  成片: 合成按钮+播放器+历史版本             | | ── 已完成  |
| [确认并  | +-------------------------------------------+ |           |
|  继续]   | | 操作条: [重新生成] [确认并继续 →]（各阶段） | | (折叠)    |
+----------+-----------------------------------------------+-----------+
```

桌面优先；最小支持宽度 1280px；<1280px 任务中心折叠为顶栏抽屉。

### 栅格与断点
- 主区自适应；资产卡片墙 3-4 列；段表格全宽
- 断点：≥1280 三栏；<1280 两栏（任务中心抽屉）；移动端非目标

### 间距与密度
- 基准 8px；表格/卡片紧凑（生产工具高密度）
- 视觉层级：当前阶段 > 确认按钮 > 任务状态 > 次要信息

## 组件

- StageNav（流水线导航）：阶段图标+状态（✓/●进行中/○未到/!失效 stale）
- ConfirmGateBar：`确认并继续`（主按钮，含前置校验提示）+ `重新生成`（次要）
- ScriptEditor：只读 active / 可编辑 draft 切换；场景折叠列表；版本下拉
- AssetCardWall / AssetCard：图预览、锚点描述、批准 checkbox、重新生成、上传替换、多视图
- SegmentTable：段行（key/时长/镜头数/引用徽标/连续性开关/状态/提示词编辑器入口）
- FrameRow（TASK-031 关键帧 Tab）：段行（锚点描述可编辑/帧图网格/批准 checkbox/上传/重抽 seed+追加提示词）；FRAME_READY 且缺批准帧时显示「缺帧段回退尾帧接力」显式勾选
- PromptEditor：等宽字体编辑器 + 结构校验实时提示 + 字符计数（≤7000）
- VideoWall / VideoCard：视频预览（hover 播放）、对比旧 Clip、重生成、首帧来源标注（关键帧/尾帧/手动）
- TaskCenter：按状态分组任务列表；进度条；失败展开错误（含 provider_code 人话解释）；重试/取消
- FilmPlayer：成片播放 + 历史版本 + 下载
- SettingsForm：供应商表单（key 打码）、模型下拉、并发步进器、[测试连接]
- EmptyState / Banner：各阶段空态与全局警告（ffmpeg 缺失、余额不足）

## 用户交互

### UI-001 确认并继续
动作：点击阶段门按钮。
结果：调对应 approve 命令；成功后流水线导航点亮下一阶段。
校验：剧本须有 draft 且 schema 通过；资产须关键资产均有 approved 图；分镜须全部段 resolution_status ≠ ref_missing 且校验通过；关键帧须逐段 ≥1 approved 帧（或显式勾选缺帧段回退尾帧接力，TASK-031）。
错误：阻断并显示缺失清单（如"林晚 缺少已批准图片 [去生成]"）。

### UI-002 一键连跑
动作：顶栏按钮。
结果：调 run_to_next_gate；按钮变为进行中状态；在确认门前自动停止。
错误：任一任务失败即停，任务中心高亮。

### UI-003 生成视频（全部/首段/单段）
动作：分镜确认后进入视频 Tab；选 scope。
结果：produce_video 命令；VideoWall 出现 pending 卡片并轮询进度。
校验：REF_MISSING 时禁用并说明；continuity 链依赖可视化（段卡片显示"等待 S01G02"）。

### UI-004 编辑已确认工件
动作：在回看 Tab 点编辑。
结果：警告对话框"将使后续阶段失效并回退"，确认后按 FR-016 执行。

### UI-005 上传首帧覆盖
动作：段详情"首帧来源"区上传图片。
结果：覆盖自动尾帧，标记 source=manual。

## 状态

每个页面/组件定义：加载中（骨架屏）、空状态（引导 CTA）、成功、错误（可重试）、禁用（前置不满足时按钮禁用+原因 tooltip）、进行中（进度/轮询）。

## 响应式行为
- <1280：任务中心 → 抽屉；资产墙 2 列
- 触控目标 ≥40px；无纯 hover 关键操作（hover 播放另有点击播放）

## 无障碍
- 语义化按钮/标题；焦点可见；对比度 ≥4.5:1；视频有字幕占位提示（H3 台词在音轨）

## 性能预期
- 首屏 <1s；任务轮询 2s 间隔（TanStack Query，窗口失焦暂停）；视频懒加载预览图
