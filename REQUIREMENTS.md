# 需求规格（Requirements Specification）—— short-dreamV4

> 来源：PROJECT_BRIEF.md（已批准，2026-09-12）

## 产品目标

让单个创作者从一句话想法出发，在一个平台内完成"剧本 → 资产 → 分镜 → 视频 → 成片"全流程，产出跨镜头角色/场景一致、带 MiniMax H3 原生音频的完整成片，全程无需离开平台手工搬运文件。

## 用户画像

- 项目所有者本人，单用户，中文界面。
- 熟悉 AI 生图/生视频的成本与质量特性，需要人工把关每个阶段的产出。
- 在 Windows 个人电脑上运行，非工程师身份使用时也能完成全流程。

## 用户故事

### US-001
作为一个创作者，我想要输入一个想法并设定题材/风格/时长/画幅，以便平台帮我规划一部短片。

### US-002
作为一个创作者，我想要平台自动生成结构化剧本（场景、角色、台词），并允许我编辑和重新生成，以便剧本符合我的意图后再往下走。

### US-003
作为一个创作者，我想要平台从剧本自动抽取角色/场景/道具资产，并自动生成设定图（也可上传替换），以便后续视频有统一的视觉参考。

### US-004
作为一个创作者，我想要平台把剧本拆成分镜：场景 → 段（一次视频生成）→ 镜头，每段自动产出 MiniMax H3 提示词，以便直接用于视频生成。

### US-005
作为一个创作者，我想要分镜声明的资产引用被自动解析成参考图并随视频任务提交，以便不用手工挑图、保证角色一致性。

### US-006
作为一个创作者，我想要同一场景内上一个视频的尾帧自动成为下一个视频的首帧参考，以便跨镜头画面连续、不跳变。

### US-007
作为一个创作者，我想要对单段视频重新生成而不影响其他段，以便低成本修掉废片。

### US-008
作为一个创作者，我想要看到所有任务的进度/失败原因/重试按钮，并支持一键连跑到下一个确认门，以便掌控整条生产线。

### US-009
作为一个创作者，我想要在全部段完成后一键合成为带声音的成片 mp4，以便直接得到可发布的成果。

### US-010
作为一个创作者，我想要配置各环节的供应商与模型（可切换），以便不被单一供应商锁定、按质量/成本权衡。

### US-011
作为一个创作者，我想要服务重启后未完成的任务能恢复或明确标记失败可重试，以便长时间视频生产不因中断而报废。

### US-012
作为一个创作者，我想要先只跑第一段视频验证风格，满意后再批量跑其余段，以便控制试错成本。

## 功能需求

### FR-001 项目管理（P0）
描述：创建项目（想法文本 + 题材/风格/目标时长/场景数/画幅/分辨率）、列表、详情、归档。
依赖：无。
验收标准：创建后项目进入 `CREATED` 状态并出现在列表；归档项目不再出现在默认列表。

### FR-002 剧本生成与确认（P0）
描述：调用 LlmProvider 生成结构化剧本（版本化草稿）；支持编辑草稿、重新生成、确认锁定 active 版本。
依赖：FR-001、LlmProvider。
验收标准：每次生成产生新版本；确认后版本不可变；生成期间 UI 显示任务进度；结构校验失败自动重试一次，仍失败则任务失败并展示原因。

### FR-003 资产抽取与管理（P0）
描述：从已确认剧本抽取角色/场景/道具资产（含外观锚点描述）；支持编辑资产描述、增删资产。
依赖：FR-002。
验收标准：抽取结果覆盖剧本中出现的具名角色/场景；资产列表可编辑；重复抽取产生新资产集版本而非覆盖。

### FR-004 资产图生成与批准（P0）
描述：为资产生成设定图（ImageProvider 适配器）；支持上传本地图替换/补充；逐图批准锁定；同一资产可有多张视图。
依赖：FR-003、ImageProvider。
验收标准：生成/上传的图先落地本地存储并立即可预览；批准状态可切换；无任何已批准图的资产在分镜引用时会阻断（见 FR-006）。

### FR-005 分镜生成与确认（P0）
描述：从剧本+资产生成分镜（场景→段→镜头），每段含：时长（4-15 整数秒）、镜头列表（切点秒数/运镜/描述/台词引用）、资产引用声明、H3 提示词初稿；支持编辑与按段重生成提示词。
依赖：FR-002、FR-003。
验收标准：段时长全部在 [4,15]；每段 H3 提示词 ≤7000 字符且含对齐指令、`[Shot k]` 切点行、`overall_soundscape`、`non_diegetic_music`、`<d>` 台词块（有台词时）；切点与镜头秒数一致。

### FR-006 参考图自动解析（P0）
描述：提交视频任务时自动把段声明的资产引用解析为已批准的资产图片；解析结果作为输入快照存入任务。
依赖：FR-004、FR-005。
验收标准：被引用资产缺少已批准图片时拒绝提交并报 `REF_MISSING`（指明缺哪张）；解析快照可追溯（记录所用图片版本）。

### FR-007 H3 视频生成（P0）
描述：经 VideoProvider（RunningHub 工作流：MiniMax H3 导演台，契约见 AI_SPEC 附录 B）逐段生成视频；一次任务承载一个段。模式经 timeline 组装表达：
- 有参考图 + 连续性开 → refs = [尾帧, 资产图…]（尾帧对齐 0.00s）
- 有参考图 + 连续性关 → refs = 资产图
- 无参考图 + 连续性开/关 → 纯文生视频 timeline（无 refs）
依赖：FR-006、VideoProvider。
验收标准：timeline 组装符合工作流契约（durationSec 4-15、refs ≤9、prompt ≤7000）；任务状态经 QUEUED/RUNNING/SUCCESS/FAILED 映射到平台任务状态。

### FR-008 尾帧→首帧连续性（P0）
描述：段级 `continuity` 开关（同场景内相邻段默认开）；前段成功后 Worker 用 ffmpeg 抽尾帧落盘，供后段按 FR-007 模式使用；支持手动上传图片覆盖自动尾帧。
依赖：FR-007、ffmpeg。
验收标准：连续性链上的段按序生成（依赖边）；前段失败/取消时后段保持 pending 不提交；尾帧文件存在且尺寸合规（≥256px、宽高比 [0.4,2.5]，超限自动裁切至合规）。

### FR-009 段级重生成（P0）
描述：对任一段发起新的视频任务（旧 Clip 保留可对比）；不重复计费已成功的任务。
依赖：FR-007。
验收标准：重生成产生新 Job 与新 Clip，旧 Clip 文件不删除。

### FR-010 成片合成（P0）
描述：全部段就绪后，按分镜顺序用 ffmpeg concat（含音轨）合成 mp4 成片。
依赖：FR-007。
验收标准：成片时长 ≈ Σ段时长（误差 <1s/段）；音轨存在；合成失败保留原因并可重试。

### FR-011 任务中心（P0）
描述：统一 jobs 表；UI 展示进度/阶段/失败原因/日志/重试/取消；Worker 重启后：running 中断的任务标记为 failed（video 类若已有 provider_task_id 则恢复轮询）。
依赖：无。
验收标准：杀掉 Worker 再启动，无永久 pending/running 幽灵任务；每个任务可查看结构化日志。

### FR-012 确认门与一键连跑（P0）
描述：项目状态机在每阶段设确认门；"一键连跑"从当前状态自动执行生成步骤直至下一确认门。
依赖：FR-002/3/4/5。
验收标准：连跑在确认门前必停；中途任一任务失败则停止连跑并报告。

### FR-013 设置（P0）
描述：供应商选择与 API Key（本地 .env/DB，界面回显打码）、各环节模型名、视频并发数（默认 1）、ffmpeg 路径检测。
依赖：无。
验收标准：Key 不出现在日志与 API 响应明文中；ffmpeg 缺失时全局提示且视频类功能禁用。

### FR-014 首段试跑（P1）
描述：分镜确认后可只入队第一段，验证风格后再批量入队其余段。
依赖：FR-007。

### FR-015 成本日志（P1）
描述：记录每次 Provider 调用（类型/模型/用量，视频按 usage.total_seconds）供估算花费。
依赖：FR-007。

### FR-016 下游失效联动（P1）
描述：确认后编辑上游工件（剧本/资产/分镜）时警告并使下游批准失效、项目回退到对应阶段。
依赖：FR-002/3/4/5。

## 非功能性需求

- 性能：单项目 ≥50 段分镜可管理；UI 列表加载 <1s（本地 SQLite）；Worker 轮询间隔可配置（默认 2s）。
- 可靠性：任务持久化，进程重启可恢复；Provider 生成的 URL（有效期短）必须立即下载落地本地。
- 安全：API Key 仅存本地（.env/本地 DB），日志脱敏；服务仅绑定本机。
- 可扩展：LLM/Image/Video 三个 Provider 均为接口 + 适配器，新增供应商不改领域代码。
- 兼容：Windows 10/11；ffmpeg 由用户安装，系统 PATH 或设置页指定路径。

## MVP 边界

### 包含
FR-001 ~ FR-013（P0 全部）、FR-014/015/016（P1，时间允许时做）。

### 排除
多用户/账号/计费、BGM 配乐混音、独立对口型控制、时间线剪辑器、云部署/对象存储、移动端、手绘分镜画板、回调（callback_url）方式接任务状态（用轮询）。

## 边界情况

- RunningHub 错误映射：433 提示词审核未过（提示改写） / 805 任务失败（failedReason 展示） / 排队与运行中（轮询） / apikey 无效或余额不足（全局提示）；MiniMax 直连错误码仅在启用备选通道（AI_SPEC 附录 A）时适用。
- 参考图超限：单段引用资产图 >9 张时按"主角>场景>道具"优先级截断并警告（不可静默）。
- 尾帧不合规（过暗/尺寸超限）：自动裁切缩放至合规；用户可手动上传覆盖。
- 图片供应商返回 URL 有效期短：生成后立即下载本地化，失败则任务失败重试。
- 剧本与资产不一致（分镜引用了不存在的资产）：分镜校验阶段拦截。
- 段时长不在 [4,15]：分镜保存时校验拒绝。
- 项目在 VIDEO_PRODUCING 时编辑分镜：按 FR-016 回退并取消未完成的视频任务（已成功 Clip 保留）。

## 待定问题（Open Questions）

- 现行图片通道为 RunningHub QWEN 文生图工作流，节点参数（宽高/seed）精确行为以 TASK-001 spike 实测为准。
- RunningHub 产物 fileUrl 为临时链接：生成后立即下载本地化，失败则任务失败重试。
- 尾帧自动择优（多帧挑最清晰）暂不做，视使用反馈。
