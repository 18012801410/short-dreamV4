# 项目状态（Project State）—— short-dreamV4

## 当前阶段
**M4 达成——全部 18 个任务完成（2026-09-13）**：规格 → 脚手架 → 领域层 → 队列 → 契约实测 → 三适配器 → 用例/REST → 工作台 UI → 真实端到端。平台已可从一句话想法产出带音轨的成片。

## 当前任务
TASK-029（端到端质量测试与自我修复迭代）已完成（2026-09-14，见 tasks/TASK-029-e2e-quality-test.md）。
建议下一步：①WorkerEngine 任务认领加原子性（多进程双跑根因）；②TASK_QUEUE_MAXED 退避加长；③「渡口夜行」重新生成分镜。

## 已完成
- 头脑风暴：定位/流程/成片边界/生图/技术栈/LLM 六题对齐，方案 A（单体+任务表）拍板
- PROJECT_BRIEF.md 已获用户批准（2026-09-12）
- MiniMax H3 V2 API 契约核实（官方文档，含互斥规则/媒体限制/错误码），见 AI_SPEC 附录 A
- 规格说明包：REQUIREMENTS / ARCHITECTURE / DOMAIN_MODEL / TECH_SPEC / API_SPEC / AI_SPEC / UI_SPEC / DESIGN_SYSTEM / AI_RULES / DEVELOPMENT_PLAN / PROJECT_STATE
- 一致性检查通过（2026-09-12，见下方记录）
- TASK-002 脚手架（2026-09-12）：FastAPI /api/system/health、React+Vite、SQLite+Alembic 基线迁移、.env 加载、data/ 目录布局；uvicorn + vite 冒烟通过，pytest/ruff 绿
- TASK-001 Spike 脚本（2026-09-12）：spike/minimax_spike.py 四步就绪（生图→r2va→互斥实测→抽尾帧），未执行
- TASK-003 领域层（2026-09-12）：server/domain 五模块（errors/enums/entities/project/job/validation）+ 40 项单测全绿；Project 十四态+ARCHIVED 迁移表、FR-016 失效回退、Job 依赖门/重试/恢复、Segment 结构不变量、H3 提示词结构校验、参考图 >9 截断
- TASK-004 仓储与队列（2026-09-12）：十表 Core 定义 + 迁移 0002（已对真实 data/app.db 执行）、Project/Job/Clip/ProviderCall 仓储、WorkerEngine（依赖排序、指数退避重试、video_gen 两阶段轮询、重启恢复）、FakeProvider 全链；49 测试全绿，alembic 升降级往返通过
- TASK-005 LlmProvider（2026-09-13）：domain/providers.py 协议（chat/chat_json/LlmResult/LlmUsage）+ adapters/llm OpenAI 兼容实现（JSON mode + Pydantic 校验 + 带错误重试 1 次 + 429/5xx 退避 + usage 透出 + 密钥不入日志）；12 项 MockTransport 单测；真实调用待 LLM_API_KEY
- TASK-006 ImageProvider（2026-09-13）：adapters/runninghub.py 共享平台客户端（create/status/outputs/cancel/upload/download/wait_for_success，超时自动 cancel）+ adapters/image.py RunningHubImage（节点 6/8/5 组装、产物落地、MediaUsage 透出）；7 项单测全绿；真实冒烟通过（9:16 老船工肖像，23 币/113s）
- TASK-007 VideoProvider（2026-09-13）：adapters/video.py（timeline 单段组装器、subject_definitions 提取为 global_prompt、帧数=duration*24+3、上传 refs 顺序即 <Picture N>、节点 7 产物、ffprobe 实测时长）+ FFmpegService 扩展（probe/尾帧/concat -c copy）；10 项单测全绿；真实冒烟通过（10s 段 → 10.125s/1920×1088/有音轨，111 币/555s）。base（纯文生）模式显式 UNSUPPORTED_MODE（模板无 t2v task_type，未实测）

## 当前架构决策
- FastAPI 单体 + React SPA + SQLite + jobs 表队列 + 独立 Worker（无中间件）
- 三 Provider 适配器：Llm（OpenAI 兼容可配）/ Image（MiniMax image 默认）/ Video（MiniMax H3）
- 视频连续性：官方 first_frame 与 reference_* 互斥 → 尾帧在有参考图时作为 reference_image[0]（提示词对齐 0.00s），无参考图时作为 first_frame
- 媒体传 H3 用 Base64 data URI（≤64MB）；任务状态用轮询不用回调
- 深色三栏工作台，桌面优先
- 领域层纯 Python + Pydantic（禁 import FastAPI/SQLAlchemy/httpx），非法迁移抛 STATE_ILLEGAL 等结构化领域错误
- DB 时间戳统一 naive UTC 落库、读回补 tzinfo=UTC（仓储层归一，SQLite 时区无关）
- Worker handler 协议：`Handler = Callable[[Job], Outcome]`，Outcome ∈ succeeded/running(+provider_task_id)/failed(+JobError)；jobs 表带排程列 next_attempt_at（仅表级，不进领域模型），退避 = base*2^(attempts-1)

## 当前领域决策
- Project 十四态状态机 + 三确认门；approve 后 active 版本不可变
- 统一 jobs 表（type/payload/status/depends_on/input_snapshot）；重试=重置同 Job，重生成=新 Job
- Segment（段）= 一次视频生成单元，duration ∈ [4,15]；镜头切点为 H3 提示词 [Shot k]
- 缺已批准参考图 → REF_MISSING 阻断，不静默降级
- **H3 提示词契约对齐官方 dialect（2026-09-12 修订，来源 MiniMax-AI/MiniMax-H3 h3-prompt-writing）**：按模式二选一——无参考图=三段（base）；有参考图（资产图或连续性尾帧）=六段（subject_definitions/summary/retention_analysis/detailed_description/overall_soundscape/non_diegetic_music）；时间线 [Shot 1] 无时间戳、[Shot N] At MM:SS.mmm 与分镜切点逐字对账；台词 <d>[语言] 逐字</d> 只在画面段时间线；结构字段恒英文。旧版「对齐首行+三段」内化作废。first_frame/first_last_frame 模式待「起始帧」槽位建模后接入（TASK-011）
- 【TASK-003 补全，待评审确认】DOMAIN_MODEL 字面迁移表之外补了两条对称/闭环迁移：
  1. `generate_storyboard` 增加 STORYBOARD_READY→STORYBOARD_DRAFTING（与 script/assets 两阶段显式的 READY→DRAFTING 重生成路径对称，否则分镜生成后无法重生成）
  2. `compose_failed`：COMPOSING→VIDEO_READY（合成失败的落点，使 compose 可重试；DOMAIN_MODEL 未写失败分支）

## 已知问题
- 端到端实测产线为 7 段（目标 ≥8）：LLM 对 45s 目标收敛到 6-8s/段 × 7 段；提高段数需在分镜提示词中强化「按 5s 切段」约束（调优项，非缺陷）
- RunningHub 同规格任务币数有波动（同 10s 段两次实测 51 vs 111 币）——provider_calls 已按实际 consumeCoins 记录，成本面板展示真实值
- H3 导演台 base（纯文生）模式未接：工作流模板只有 r2v task_type 字符串，t2v 串未实测——VideoProvider 对无参考图的段显式抛 UNSUPPORTED_MODE；待一次廉价真实 t2v 实测钉死字符串后接入
- Worker 单进程串行（视频并发 1）；编辑确认后工件的 stale 标记简化为项目状态回退（工件保留不删）
- RunningHub create 响应含 netWssUrl（WebSocket 进度通道），MVP 不用（轮询即可）
- MiniMax 官方直连（AI_SPEC 附录 A）降级为备选，未被采用
- **存量分镜编号错位（TASK-018 修复后的迁移项）**：TASK-018 之前生成的项目（如「渡口夜行」active 分镜）按旧惯例编号，连续段提示词与实际参考图错位一号（变脸根因）；UI 分镜 Tab 现以「⚠ 编号错位」徽标标出，需对该项目重新生成分镜才会按新规则产出

## 待定问题（Open Questions）
- 首帧/首尾帧模式（三段式 + 逐字对齐句）待平台建模「起始帧」槽位后在 TASK-011 接入
- 尾帧自动择优（多帧挑最清晰）暂不做，视使用反馈。

## 阻塞项
- 无。LLM key 已配置（实测为 DeepSeek 的 key，非 GLM；用户认可即沿用）。

## 近期变更
- 2026-09-17：**TASK-047/048 系列分集 + 剧本提质（用户需求："剧本太短、要一集一集、要符合大众口味"）**：根因——①全项目无 episode/series 概念（project=一条 ~60s 短片），且 `target_duration_sec=60/scene_count=3` 前端根本改不了；②剧本提示词 hook 轨缺行业标准打法、单句台词允许 50 字、H4「禁止跨集悬置」与连载互斥。**方案 A（系列化）**：新实体 Series/SeriesParams/EpisodeOutline/SeriesOutline（domain/entities）+ SeriesStatus 枚举 + 迁移 0006（series 表 + projects.series_id/episode_no，存量项目空值默认行为不变）+ SeriesRepo；SeriesAgent 两个新方法（agents.py）——`run_series_outline`（先全季大纲后逐集剧本，>12 集自动分批续写，产出后确定性重排集号/截断到请求数）与 `run_episode_script`（全剧人物表+本集钩子/梗概/爽点/卡点+上集卡点衔接驱动，连载法则 `_SERIES_LAWS`：X1 承接上集/X2 钩子卡点落地/X3 跨集悬置许可但每集必须有本集内小对抗释放/X4 人物一致/X5 信息配给）；新命令族 `POST /series/{sid}/commands`（generate_series_outline→`series_outline_gen` Job；edit_series_outline 每次重跑质量门，CREATED/APPROVED→READY；approve_series_outline 硬门 422 硬拦；generate_episode_scripts 为缺项目的集各建档入队 script_gen（payload 带 series_id/episode_no，Worker 天然并行/独立重试）；inherit_series_assets 跨集零成本复制资产卡（同一份磁盘文件），目标集直接落 ASSET_READY）；确定性质量门 `server/domain/outline_quality.py`（零 LLM：硬门=集号连续/集数一致/每集钩子必填/卡点除大结局必填/第 1 集必有爽点/大爆点不压最后一集（≥6 集才硬拦，短篇降软门）/大爆点≠最后一集，软门=爽点真空≤3集/人物≤10/梗概厚度/卡点不同文）；前端新页 `/series`（创建表单含集数×每集秒数、大纲逐集卡片编辑+质量门横幅、分集列表带工作台链接与继承资产按钮）+ 首页「系列短剧」入口与分集徽标。**方案 B（提质，两轨受益）**：hook 法则按 dj-short-drama 方法论+行业共识重写（黄金 3 秒四模板开场/反转密度铺垫≤20%/爽点前置≤40%/结尾卡点定格/台词单句 8-20 字硬上限 25 字禁解释腔），`update_params` 开放 target_duration_sec/scene_count，创建项目表单同步补输入。文档 AI_SPEC（SeriesAgent 节+hook 法则修订）/DOMAIN_MODEL（Series 实体+Project 分集字段）/API_SPEC（API-011）/UI_SPEC（/series 页）同步。**验证**：后端 235 项（220 存量+15 新增 test_series.py）/ 前端 16 项全绿，tsc+vite build 通过；迁移 0006 已对真实 data/app.db 执行（备份 `data/backup_appdb_before_series_20260917.db`，12 表行数核对一致）；重启后 `stale:false`。**真实 LLM 全链验收（全程零币）**：建 3 集×60s 验收系列《赘婿归来》→ 大纲生成 ✅（剧名/logline/5 人人物表带公开身份 vs 真实身份/每集钩子·爽点·卡点齐备；质量门真实拦截一次「大爆点压最后一集」，据此把该门在 <6 集短篇降为软门）→ 确认大纲 → 批量生成 3 集剧本 ✅ 全部 SCRIPT_READY；抽查：钩子/卡点落进首末节拍、台词最长 16/27/13 字（口语化生效）、第 3 集首版 145s 超时长被 warnings 抓到→免费重摇一次收敛到 90s（仍 +50%，护栏如实提示、草稿门人工决策）。**遗留**：①剧本时长偏差目前只 warning 不自动重摇（可做确定性自动重试，待用户反馈）；②「第 N 集重写剧本」需进该项目工作台点「生成剧本」（系列页对已建档集不提供重建按钮，防误覆盖）；③出片成本与单项目一致，系列页不做聚合计费。**UI 二次调整（用户反馈两条）**：①首页每部剧聚合为一张卡（剧名/系列状态/已建档 X/N 集/「查看分集」→ /series/:sid 直达），系列单集不再单列卡片；首页「项目」区只显示独立项目；②「逝去的童年」项目按用户指令归档（可逆，未删数据）。**三次调整：系列资产自动继承（用户反馈"每集都要重抽资产，无法保证一致性"）**：`cmd_generate_assets` 升级——同系列存在「有已批准且已落盘图」的兄弟集（集号最小者优先）时，点「抽取资产」自动改为**免费继承**：整体替换目标集现有资产（复用重新抽取的 delete+copy 口径，顺带解决第3集 15 张旧漂移卡的清理）+ 复制来源卡片与图片行（同一份磁盘文件），直接落 ASSET_READY；`force_extract=true` 才走真抽取。手动 `inherit_series_assets` 命令同口径重构。事故链复盘：第3集资产抽取（15 张 ≈300 币）源于①用户在首页旧列表点错集②第3集剧本是压时长重摇版、人物名整体漂移（李桂芬/王雪/陈国栋/老管家）→ 资产卡全是"陌生人"。修复：第3集剧本人名确定性对齐系列口径（免费新草稿，待确认）；第1集确认剧本+抽取 8 张（**实录 101 币**），苏母（王秀兰）/苏瑶卡齐备。后端 236 项全绿（新增自动继承测试）；改 usecases.py 后已重启 API+Worker。**四次调整：人物/道具唯一命名归一（用户硬要求"同一人物或道具不得出现多个命名"，即第3集 李桂芬/王雪 式漂移的机制性根治）**：新模块 `server/domain/name_registry.py` 确定性归一（零 LLM）——①系列模式先对齐大纲登记名（别名等价=全名/括号注名/去括号基名，对得上就改写、对不上 warning 交人工）；②剧本内重复合名合并（规范名=台词说话人最多者，并列取先登记，profile 合并）；③全文单遍改写（dialogue.speaker/beats.text/scenes.summary/logline 的别名→规范名，长别名优先正则交替避免套娃）；④props 去重（基名相同合并，互为子串给疑似别名 warning）；未登记说话人 warning。提示词加硬规则 8（唯一命名）+ 连载法则 X4 强化（人物名逐字用大纲登记名）。后端 242 项全绿（新增 test_name_registry.py 6 项）。**五次调整：第1集分镜两次确定性校验失败的根治**：worker 日志实锤两类病因——①提示词让 LLM 写「每段 shots 1-3 个」而校验器硬限 ≤2（自相矛盾必失败）；②subject_placements 写了在场角色但 asset_refs 漏带该角色卡（苏瑶/苏母（王秀兰）），且 placement 名未用资产精确名。修复：①提示词改为「shots 1-2 个（硬上限 2）」；②`_autorepair_scene` 新增在场角色对账自愈（确定性：placement 名按别名等价对齐资产精确名〔names_match〕、asset_refs 缺在场角色卡自动补、匹配不到任何角色资产的幻觉 placement 移除）；③`error_outcome` 把 ValidationFailedError 的 errors 明细并入 JobError.message（页面直接看到具体失败项，不用翻 worker 日志）。重试第1集分镜成功：6 段 68s（目标 60s，+13% 在容差内），全部段 placements 逐人精确名、镜头 ≤2。后端 242 项全绿；改 agents.py/handlers.py 后已重启（版本 08f816ae1337）。**六次调整：图片生成并发可配置（用户指令"做一个配置项我自己设置并发，默认2"）**：出图慢的双瓶颈实测——①3 Worker 串行占坑（8 张图入队→领取最长 370s）②RunningHub 账号并发队列满拒单（TASK_QUEUE_MAXED → 120s 退避循环，被拒=未提交=未扣币）。处置：①Worker 池 3→6（一次重启未先查在途、2 个关键帧被判中断重排——违反 AGENTS.md 第 2 条，已认错，被重排任务无计费记录但仍可能有平台侧孤儿消耗待用户控制台核对）；②新配置 `image_concurrency`（默认 2）：`.env` 可设 + **设置页可改**（SettingsBody/GET /settings/SettingsPage 输入框），引擎领取闸门 `claim_next_runnable(type_gate=...)` + `WorkerEngine(image_concurrency_provider=...)` 每轮现场读 settings 表覆盖（改完即生效无需重启），作用范围 image_gen+frame_gen，达到上限的任务留在 pending 不耗重试预算，非图片类任务不受影响。后端 244 项全绿（新增闸门测试 2 项）；已重启生效。加速工作流方案已调研告知用户（Nunchaku/8 步 LoRA/Rapid AIO，保持节点 6/7/10/11/12/8/5 布局换 RUNNINGHUB_WORKFLOW_IMAGE_EDIT 即可）。**七次调整（用户反馈两条：①重新生成图片一模一样没变化 ②改提示词想用中文）**：①种子根因——前端重生成不传 seed，后端 payload 就不带 seed → 工作流用保存的固定种子 → 同提示词同参考图必出同图。修复：`cmd_generate_asset_image` 与 `_enqueue_frame_job`（覆盖 generate_keyframes/generate_frame_image 全路径）未显式传 seed 时**后端随机化**（`_random_seed()`，2^31 空间）；显式 seed 仍可锁定复现。②新端点 `POST /projects/{pid}/translate_prompt {text}`（LLM 免费翻译，不落库，结果由页面填框确认后使用）+ `WorkbenchService.translate_prompt`（懒加载缓存 LLM）；前端新组件 `ZhPromptBox`（中文 textarea + 「中文 → 英文提示词」按钮）接入三处：资产卡自定义提示词框下方、资产编辑的每条 image_plan 设定图、关键帧面板提示词框下方（翻译结果覆盖整段提示词）。后端 247 项全绿（新增 test_seed_and_translate 3 项）。**八次调整（用户反馈：翻译不要无脑直译，要按 Qwen 官方规则写提示词）**：调研阿里云百炼官方《文生图 Prompt 指南》（公式=主体（特征+动作）→场景→风格→镜头语言→氛围→细节修饰，自然短句非关键词堆砌，中英双语，完整度越高质量越高）+ Qwen 文字渲染社区共识（画面内文字用英文双引号精确包裹+字体颜色位置）。`translate_prompt` 升级为双模式：**t2i**（资产卡，按官方公式改写）/**edit**（关键帧图生图=Qwen-Image-Edit 指令式：编辑动词开头只说改动 + 末尾固定 Keep everything else…unchanged 保留子句），两种模式画面内文字均保留中文原文双引号包裹。ZhPromptBox 加 mode 属性，关键帧处传 edit。实测：t2i 输出按公式分段（主体→场景→风格→镜头→氛围→8k 画质词，中文标题引号内保留）；edit 输出 Replace…+Keep everything else 尾句。后端 247 项全绿，已重启（版本 18ba0456b34e）。**九次调整（用户反馈：不会写提示词，希望直接说「场景中有2个桌子」这种改动）**：`translate_prompt` 增加 `current_prompt` 参数走**智能修改分支**——现有提示词 + 中文改动要求一起给 LLM，规则：只落实要求的改动、其余逐字保留、精确消除冲突描述（要求 2 桌则移除 a single table）、画面内文字保留中文、按官方公式组织。前端 ZhPromptBox 接收 currentPrompt 自动切换语义（有基准 → 「把我的修改融入提示词」/无 → 「生成英文提示词」），三处调用（资产卡自定义框/图计划每条/关键帧面板）均传当前值。实测用户案例：旧提示词一张桌子 + "场景中有2个桌子" → two round rosewood banquet tables，窗户/灯光/空镜/画质词逐字保留，single 冲突描述已消除。后端 247 项全绿；已重启（版本 7a15e0025bef）。另：界面文案澄清 Qwen 原生支持中文提示词（"英文效果更好"等误导文案已改，翻译按钮改为可选润色工具）。**十次调整（用户反馈两条：①改了资产后关键帧不更新、还显示原来的 ②关键帧也要像资产那样支持中文修改，且输入框太多）**：①根因修复——`_pick_primary_image` 原实现同标签层取**最旧**批准图（注释明言"不取最新"），用户重画批准新图后关键帧/视频仍引用旧形象；改为同标签层取**最新批准**（approved 按 created_at 升序取末位）。②关键帧卡 UX 重构对齐资产卡：砍掉 seed/追加英文提示词/重抽 三个零散输入，统一为一个智能框（有字=按 Qwen-Image-Edit 指令规则智能融入当前提示词并覆盖；留空=按**最新批准资产图**随机重生成）+「高级：提示词/参考图/seed」折叠面板（seed 移入）；新增**资产图更新徽标**（帧的 reference_paths 与当前自动选择不一致时提示"资产图已更新，建议重做"）。后端 248 项全绿（新增主图选择测试）；已重启。**十一次调整（用户需求：关键帧页加「重新抽取关键帧」总按钮）**：FramesTab 头部新增「重新生成全部关键帧」按钮——调既有 generate_keyframes（全段重新入队，提示词按当前编译器规则实时编译、参考图自动取最新批准资产卡；旧版本保留可对比），带确认框（段数×约40币预估）；忙碌态由既有守卫拒绝。纯前端改动，vite 热更。
- 2026-09-17：**TASK-045 补充：16 段 v2 关键帧全量重出对比完成**：S02G02/04/05 描述先改逐人具名（edit_segment → FR-016 分镜失效 → 重approve → generate_keyframes）。结论：**克隆人/换装/人被替换三类问题全部消失**——S02G05 三人齐且各自正确（v1 是乙穿甲的红条纹衣+只画 2 人）、S02G02 乙单人跳皮筋干净无双胞胎、13 段单人零退化；**残留**：S02G04 声明 3 人只画 2 个（甲缺席），"Exactly N people" 正向声明不保证计数——多人段需逐张目检、不行重摇。花费：Edit 关键帧约 43 币/张，14 次已记录 599 币、16 张全程约 685 币。详见 tasks/TASK-045-keyframe-qwen-edit.md
- 2026-09-17：**TASK-045 关键帧"换脸/换装/多人"三症候群：联网调研 + 三个结构性根因修复**：用户反馈关键帧"人物变了、衣服变了、多个人"，要求彻查。**诊断（逝去的童年 16 段全量核对 + 成图/参考卡目检）**：①S02G04/S02G05 声明 3 人只传 2 张角色卡——小伙伴乙（黄裙马尾）**没卡 → 直接消失**，右侧被画成第二个小伙伴甲（同款红条纹衣）；根因是 Edit 工作流只有 3 个参考槽而旧逻辑固定"角色 ≤2 + 场景卡"。②S02G02 描述写"Two girls…A third girl…"（无名人物）且只有乙一人有卡，参考槽**复制乙的卡**补满 → 画出一对双胞胎乙、第三人消失。③提示词写错了读者：`[img0]（陈末）的脸部特征…` 这类占位符**ComfyUI 原生不解析**（`TextEncodeQwenImageEditPlus` 把参考图按槽位编码为 "Picture 1/2/3" 再原样拼接正文，comfy_extras/nodes_qwen.py 证实），`[img0]` 等于噪声；"方向性物体必须方向自洽：写清…"等元指令是对分镜作者说的话，扩散模型只当噪声；且该模型**负向词基本无效**（官方示例只给一个空格，issue #120），KEYFRAME_NEGATIVE 拦不住"多个人"；多人身份融合本就是 2509 已知弱项（官方一致性承诺仅单图章节，issue #88、nunchaku#618，2511 版侧面承认）。单人段 13/16 全部正常（脸/服装/场景贴合），问题全部集中在多人段。**修复**：①参考卡槽位——在场角色 ≥3 时 3 槽全给角色卡、场景卡让位（场景一致性由视频阶段承担）；≤2 保持角色+场景；②补槽复制**最后一张**（场景卡），不再复制人物卡（复制人会"复印"人）；③分镜新规 C8 + 校验器 `_KEYFRAME_UNNAMED_PEOPLE_RE`：keyframe_description 可见人物必须逐人具名（in_frame=true 的角色资产），无名人物（two girls/a third boy/a crowd）判 VALIDATION_FAILED 进重试（真实事故原句全命中，two bicycles/her two hands 不误伤）；④**关键帧提示词编译器重写**：参考图用 **Picture N** 指称、人数正向声明 `Exactly N people…, no other people`、身份锁定一句话 "Keep every character's face, hairstyle and clothing exactly the same as in their Picture reference."、删除全部元指令，约 1300 → 717 字符。**验证**：后端 220 项 / 前端 16 项全绿；重启后 API/Worker 均 `stale:false, version:f88c73d04c3d`（队列空闲时重启，0 中断）。**遗留**：存量 16 张旧提示词关键帧不会自动变好——单人段按需重出；多人段（S02G02/04/05）先在分镜页把 keyframe_description 改成逐人具名再重出（keyframe_description 在编辑白名单内）；若多人段仍不稳，升级路径＝Qwen-Image-Edit-2511 / 两段法（先构图后修脸，issue #88）/ prompt rewriter 前置。详见 tasks/TASK-045-keyframe-qwen-edit.md（含来源清单）
- 2026-09-16：**TASK-044 进程旧代码静默烧钱 → 代码新鲜度护栏（事故复盘 + 根治）**：用户问"不是改成全身照了吗，为什么生成的还是半身照"——根因是**长驻进程跑的是旧代码**：3 个 Worker 19:54 启动、API 22:36 启动，而 TASK-042/043 的源码改动发生在 22:39–22:52（uvicorn/worker 都没开 `--reload`，改代码不生效），所以 22:57 用户点「抽取资产」时，**旧 Worker 里的旧 AssetAgent** 按旧口径写了 4 张"半身定妆像"+2 张"三面板空镜"的提示词，12 张图照常生成、照常扣币，**页面看不出任何异常**。直接损失（provider_calls 实录）：本轮 12 张共 107 币，其中错误口径 89 币（4 张半身 63 + 2 张三面板 26），6 张道具卡 18 币属正常花费。**治理**：①新模块 `server/infra/buildinfo.py` 确定性判定"源码最后修改时间晚于进程启动时间 = 跑旧代码"（含源码指纹 version、新旧时间戳、可照做的中文处置文案；读不到文件时按新鲜处理，辅助网不堵正常工作）；②**付费命令在花钱前硬拦**——API 侧 `dispatch` 对 `generate_assets/generate_asset_image/generate_keyframes/generate_frame_image/produce_video/regenerate_segment` 抛 `STALE_CODE`（免费动作不受影响），Worker 侧 `handle_asset_extract` 在任何 Agent 调用前拒绝（这步最贵：1 次抽取 → 12 张图）；③可见性：`/api/system/health` 增 `code:{version,stale,process_started_ts,newest_source_ts,newest_source_path}`、Worker 启动打代码版本横幅（旧代码 WARNING）、首页状态条加「代码 xxx」/红字「后端代码已过期，需重启」+ 红框给出重启命令；④逃生口 `ALLOW_STALE_CODE=1`。**规则化**：新建仓库根 **AGENTS.md**（本项目此前没有）——第 1 节「改完代码必须重启 API 与 Worker」含照抄可用的三组命令（查在途任务 → 停/起 Worker 池 + API → 确认 `stale=false`）、事故证据、两条易混路径（「点某张卡生成」读库里提示词、不受重启影响；「抽取资产」进程现场撰写、必须重启）与存量提示词免费重写命令；第 2 节「花钱前自查、花钱后如实报账」；第 3 节「删改用户数据前先看目标先备份（SQLite 在线备份 + 逐表行数核对）」；AI_RULES.md 保持原样不再重复该规则，PROJECT_STATE 交接备注改指 AGENTS.md。**本次处置**：恢复被我误取消的一条成功任务（读到"运行中"时它其实已完成，已还原 job=succeeded、图片行=ready）；把这个项目的 6 条旧口径提示词**免费**改成新规则（备份 `data/backup_prompts_before_regen_20260916.json`），重启后 API/Worker 均报 `stale:false, version:da25b30c8f22`。**验证**：后端 216 项 / 前端 16 项全绿。**遗留**：那 6 张旧口径卡（89 币）仍可用，换新口径需页面逐个点「生成」（约 80–90 币）；重启回收把 2 个刚认领的道具任务判为中断，RunningHub 侧可能已计费但无记录（潜在未入账约 18 币）。详见 tasks/TASK-044-stale-code-guard.md
- 2026-09-16：**TASK-043 角色卡从「正面半身定妆像」改为「正面全身定妆照」**：用户指令"人物也应该改成正面全身照"。这条早有 A/B 结论——EP001 同角色同 seed 的半身 vs 全身对照里，**全身版裤子/外套下摆/鞋的证据完整、脸部仍清晰**，半身版下半身无据可依，当时建议改全身待用户拍板，本次落地。**改动**：①AssetAgent 角色卡规则改为单幅正面平视全身定妆照（站直、正对镜头、双臂自然垂放、头到脚完整不裁头顶与鞋、人物占满画面高度并居中、服装从鞋到配饰完整可见；仍是一图一人、只画人物本身、严禁拼贴/多视图/三视图），确定性兜底卡同步改全身；②`_CARD_POSE_HINT_RE` 增加全身取景词（全身/立绘/full-body/full-length/head-to-toe）——取景属卡片本身，留在 visual_anchor 会把剧情镜头锁成全身站姿，编译期确定性剥离；③关键帧身份锁行补「角色参考图只用于身份——脸部特征与服装；本帧的景别、姿势与构图按上面的画面描述来」，防止全身卡作为 Edit 参考图把特写拽成全身（与 TASK-042 场景行同一机制）；④存量脚本角色模板同步 + `--scenes-only` 泛化为 `--kind=character,scene,prop` + **新增空 image_plan 兜底**（没有计划项的资产点「生成」时用的是裸锚点、不带动画幅与卡片取景，实测命中 1 个：雷震）。**验证**：后端 211 项 / 前端 16 项全绿，ruff + tsc 通过；存量 8 个项目 32 条角色提示词 + 1 条补卡已落库（**未删图/未动批准/未花币**，旧词备份 `data/backup_character_prompts_20260916.json`）。**取舍**：全身卡脸部像素占比低于半身卡（9:16 实测脸仍清晰；16:9 横版若出现脸不稳，该项目可单独在页面改提示词重抽半身）。**待用户拍板的花钱项**：存量角色卡图片仍是半身像，需在资产页点「生成」重出（约 20-25 币/张）再重出受影响关键帧与视频。详见 tasks/TASK-043-character-card-full-body.md
- 2026-09-16：**TASK-042 场景卡从「多视角拼贴」改为「单幅广角全貌」**：用户反馈"关键帧生成不好，感觉是资产图片生成不好，场景不要用多视图了，能不能用广角镜头覆盖场景大部分区域"。免费取证确认病灶：9:16 项目的竖排三面板空镜卡被画成**细条 + 大片黑边**（末班车驾驶座卡三个面板都只拍到驾驶区一角），16:9 的婚宴大厅卡虽然画得好但本质是一张**设定表而非一个空间**——关键帧 Edit 图生图拿到的是三个互相矛盾的机位，`以 [imgN] 为准` 无法指向哪一面板，且关键帧负向词里恰好写着 split screen/collage（**参考图与负向词自相打架**）；H3 视频拿到的也是拼贴而不是一个镜头。**改动**：①AssetAgent 场景卡规则重写为"整张卡只有一幅画面"——广角/超广角（约 16-24mm）从看见全貌的机位拍摄（室内取角落或略高于视线，街道取一端/对面临街），空间绝大部分一次性入画、地标同框、纵深层次清楚，禁止分格/拼贴/多面板/多机位/平面图（`scene_layout` 三面板变量删除）；②场景锚点新增禁写「广角/机位/视角/构图/全景」等取景词（取景属卡片本身，写进锚点会把后续每个镜头的景别锁成广角远景），编译期 `sanitize_scene_anchor` 增加取景词消毒兜底；③关键帧场景行改为"以 [imgN] 为准（该参考图是同一空间的广角全貌：地标位置、朝向与相互距离以它为准；本帧的景别与构图按上面的画面描述来）"——参考图只当空间依据、仍不给不变锁（TASK-037/038 教训）；④`IMAGE_NEGATIVE_SCENE` 加拦 split screen/collage/multiple panels/contact sheet/letterbox bars，确定性保证卡是单幅；⑤存量维护脚本 `normalize_asset_cards.py` 模板同步新口径（角色分支顺带修正为**正面半身定妆像**——旧模板还在出四视图，与 TASK-030 后的默认不一致）+ 新增 `--scenes-only` / `--prompts-only` 开关。**验证**：后端 208 项 / 前端 16 项全绿，真实段编译目检通过；存量 8 个项目 22 条场景提示词已按新口径落库（**未删图/未动批准/未花币**，旧词全文备份 `data/backup_scene_prompts_20260916.json`）。**待用户拍板的花钱项**：存量场景卡图片仍是多视图，需在资产页点「生成」重出（约 23 币/张）再重出受影响关键帧（Edit 版约 48 币/张）。详见 tasks/TASK-042-scene-card-single-wide-shot.md
- 2026-09-16：**TASK-041 镜头内容改中文展示（英文原文折叠）**：用户要求把关键帧页那段英文镜头描述改成中文。做法是"展示层中文、生成层不动"——镜头动作 `shots[].action` 是英文生成素材（逐字进 H3 英文正文），但每镜都记着认领的剧本节拍（`shot.beat_refs`），而剧本节拍本身是中文（用户已确认）。新增 `WorkbenchService.shot_beats_zh(pid, segments)`（节拍原文解析，含 音效/画面文字/转场 前缀与台词"说话人：台词（语气）"），`/frames` 与 `/storyboard` 均返回 `shot_beats_zh`；前端关键帧页与分镜页每镜改显示中文节拍，英文动作段收进「英文原文（送视频提示词的动作段）」折叠项（默认收起），节拍缺失时回退英文。后端 204 项 / 前端 16 项全绿，浏览器实测两页中文可见、英文默认折叠。**补丁**：用户发现镜1/镜2 显示同一条节拍（实为两镜认领同一拍、景别不同），中文展示未去重 → 新增 `dedupeShotBeats`：本镜节拍被上一镜全覆盖（相等或子集）时显示"（与上一镜同属一拍，景别：…）"，两页共用；实测 S01G01/S01G02 的镜2 均已正确标注。注意：提示词层（H3 六段与关键帧指令）仍是英文——结构字段官方要求英文；整体改中文需实现 `prompt_lang=zh`（当前空实现）并重新生成分镜。详见 tasks/TASK-033-034-keyframe-reference-and-concurrency.md
- 2026-09-16：**TASK-040 视频提示词可手改（人工覆盖）+ 展示即生产文本**：用户问"这块是视频提示词吗"时发现分镜 Tab 可编辑的提示词框在生产时会被实时重编译覆盖（TASK-032 副作用），页面缺人工覆盖入口。改动：`H3Prompt.manual_override` 字段 + `cmd_edit_segment` 改正文即置覆盖（`reset_prompt=true` 服务端重编译并清除）+ `_compile_production_prompt` 遇覆盖直接采用存储文本 + 分镜 Tab 显示「实时编译」标识 / 「人工覆盖（生成时不再重编译）」徽标 / 「恢复自动编译」按钮。**顺带修两个真问题**：①`cmd_edit_segment` 旧实现只对 h3_prompt/shots 做嵌套建模，asset_refs 留在 dict 状态会让 `validate_segment` 抛 AttributeError（页面「保存段修改」会踩）→ 改为白名单过滤 UI 只读字段后整段 `model_validate`；②`segment_reference_previews` 用存储缓存文本核对 `<Picture>` 编号，关键帧批准后会误报「⚠ 编号错位」（实测 S01G01：6 张参考图 vs 文本只提 1–5）→ 预览与页面展示统一改为**生产实际会送出的文本**（未覆盖则实时编译），假告警消失。后端 204 项 / 前端 16 项全绿，浏览器实测徽标、按钮与告警消失。详见 tasks/TASK-033-034-keyframe-reference-and-concurrency.md
- 2026-09-16：**TASK-039 场景卡 A/B 定论 + 构图细则与方向性物体入提示词**：①用户选修"2"，跑 S2 场景 4 段 × 2 变体对照（同版提示词，只差参考图有无场景卡）：**A 组四段是同一辆车且贴合场景卡**（灰金属立柱+蓝吊环+冷白 LED），**B 组是另一种通用公交**（黄立柱+印花布座椅）且彼此更散、还出现 1 例多余人影 → **结论：场景卡要传**（当前默认正确，不改代码）；此前"空镜卡把人挤走"的判断系混杂（同时换了提示词版本），真凶是"保留空间结构不变"那句锁（TASK-037 已修）。附带：B 组现金画成美元（道具卡被排除、3 个参考槽位占满，币种只剩文字），可在页面改提示词重抽。②按用户"动手"把豆包 frame.md 余下部分并入关键帧提示词（零生成成本）：**按画幅的构图细则**（竖屏人脸/上半身安全区、关键道具中下部、双人左右关系、不贴边；横屏三分法、视线呼吸空间、纵深、180° 轴线）+ **方向性物体六要素**（手机/镜子/后视镜/车门/现金等命中时要求人物朝向、视线、物体朝向、手部接触、可见面、距离自洽），标签只输出物体名不截句子。后端 203 项全绿。详见 tasks/TASK-033-034-keyframe-reference-and-concurrency.md
- 2026-09-16：**TASK-038 关键帧链式锚定：实测否决（负结果记录）**：用户问"场景不传，能保证不漂移吗"。先做免费核查——**传了场景卡，同场景各段仍在重造空间**（S02G01 粗黄扶手柱+蓝座椅 vs S02G02 灰金属扶手+红 LED；S01G01 长灯带 vs S01G04 圆形小灯），说明空镜卡对空间约束力很弱。于是尝试链式锚定：同场景紧邻前段的已批准关键帧作参考图、非链头不传场景卡。两次实测（S01G02 应为后视镜特写）：锚点作 [img0] → 车内一致但**构图被锁死**（画成上一段的侧拍司机）；锚点放末位 → 仍锁构图。结论：该图生图模型只要见到上一帧，其构图就压过本帧文字要求，锁死镜头等于毁掉剪辑节奏 → **否决**，参考图固定为"画面内角色卡(≤2)+场景卡"，并加回归测试防止回加；场景一致性交回视频阶段（H3 带场景卡 + 本帧作 Picture 1）与连续段尾帧接力。后端 201 项全绿。详见 tasks/TASK-033-034-keyframe-reference-and-concurrency.md
- 2026-09-16：**TASK-037 关键帧对齐豆包 frame.md（I2I 编辑指令）**：用户指出关键帧"还是不理想"并给出豆包 `doubao-creative-drama/references/frame.md` 参照。差异核心：豆包的关键帧 prompt 是**图生图编辑指令**（正文显式引用 `[img0]/[img1]`、逐张声明保留项、空间线性顺序"背景→主体→局部→道具"、结尾写画幅、风格词与剧本头一字不差），我们原来写的是面向文生图的画面陈述、图是隐形输入。**两次实测**：①按豆包格式全量重写（含"保留 [img2] 场景空间结构不变"）反而更差——司机坐到乘客位、驾驶座空着还多出戴口罩的人；根因是空镜场景卡被"保持不变"锁住（空座位不许动，人只能被挤走），与 TASK-036 的空镜偏置同源、被锁放大。②修正定稿：保留画面描述主干，只吸收豆包真正有价值的部分——**身份绑定居限于角色**（`保持这些人物的身份不变：[img0]（陈默）的脸部特征、发型与服装结构（深蓝色旧夹克…）`）、场景卡只写"以 [img2] 为准"不给不变锁、顺序改为 background→N people in frame→visible props、结尾补画幅与画质。S01G01 v8 复测：司机正确在驾驶座、恰两人、无多余人物；遗留被遮挡次要角色的服装漂移。后端 200 项全绿。详见 tasks/TASK-033-034-keyframe-reference-and-concurrency.md
- 2026-09-16：**TASK-035/036 关键帧可看可改 + 提示词改画面描述式**：用户反馈"关键帧页看不到提示词/参考图也不能改"、"生图提示词跟视频提示词一样，应该描述画面内容"。TASK-035：`SegmentFrameImage.reference_paths`（迁移 0005，生成完成时按任务实际参考图回填）+ `generate_frame_image` 支持 `prompt` 与 `reference_asset_image_ids` 覆盖 + `/frames` 返回 `segment_context`（分镜各镜动作/逐人位置与 in_frame/自动参考图）与 `reference_options`；FramesTab 每段摊开分镜内容、提示词全文（可改/可恢复自动）、参考图缩略图多选（自动项带标）与"用以上提示词与参考图重抽"。顺手修：①项目卡片生成中带 `pointer-events-none` 导致**整个项目进不去**（用户实测），改「查看（进行中）」可点；②历史帧无参考图记录时面板会全空、点重抽会静默变纯文本生图——现回退到本段自动选择并黄字提示。TASK-036：关键帧提示词从"锁定指令清单"（元指令 + 大段中文身份串 + 双 `setting:` 打架）重排为画面描述（`Cinematic still frame: {画面}` + `N people in frame - 谁在哪` + 单 `location:` + `visible props:`），有角色卡时正文不写身份串。实测 S01G01：旧格式+空镜场景卡会把司机画到过道上；只传角色卡的对照实验与新格式都正确落在驾驶座、画面恰两人。后端 197 项/前端 16 项全绿；取消 10 个旧格式排队任务（省约 480 币）后重派 13 张新格式帧。详见 tasks/TASK-033-034-keyframe-reference-and-concurrency.md
- 2026-09-12：生成全套规格说明包；简报 D5 依据官方契约更新
- 2026-09-12：TASK-002 脚手架落地并冒烟通过
- 2026-09-12：TASK-003 领域层落地（含上述两条状态机补全）
- 2026-09-12：TASK-004 仓储与队列落地（迁移 0002 已应用到 data/app.db；Worker 从心跳占位接入 WorkerEngine）
- 2026-09-12：**供应商变更（用户拍板）**：视频/图片改走 RunningHub 工作流 API；规格五件（BRIEF/REQUIREMENTS/ARCHITECTURE/TECH_SPEC/AI_SPEC 附录B）同步更新；.env/config 改 RUNNINGHUB_* 配置；spike/runninghub_spike.py 实测——文生图端到端 ✅（21 币/104s）、上传鉴权勘误（只认 Bearer 头）✅、视频工作流 ID 无效（810）❌ 待用户核实
- 2026-09-12：**H3 提示词契约修订（对齐官方 dialect）**：AI_SPEC 规范重写（模式矩阵/三段六段/时间线记法/说话人四硬线）、DOMAIN_MODEL Segment 不变量更新、validation.py 改为模式感知校验、测试扩到 61 项全绿
- 2026-09-13：**TASK-001 完成**：视频工作流 ID 勘误为 2093160296864116737（节点 12/输出节点 7），六段式提示词 + 单参考图端到端跑通——成片 10.125s/1920×1088/有音轨，51 币；全部实测结论回写 AI_SPEC 附录 B；M1 里程碑达成
- 2026-09-13：TASK-005 LlmProvider 落地；TECH_SPEC「httpx async」修订为同步（Worker 同步 handler、逐 Job 串行）
- 2026-09-13：LLM key 配置并真实冒烟通过——用户提供的 key 实测是 DeepSeek 的（sk- 格式，GLM 端点 401，DeepSeek 端点通过），`.env` 切到 `https://api.deepseek.com` + `deepseek-chat`；JSON mode 结构化输出一次通过（剧本级冒烟 113 tokens）。`.env.example` 补了双平台端点对照
- 2026-09-13：TASK-006 ImageProvider 落地并真实冒烟通过；RunningHub 共享客户端从 spike 契约产品化（TASK-007 复用）
## 近期变更
- 2026-09-16：**TASK-033/034 关键帧参考图隔离 + 并发 Worker 提速（用户反馈驱动）**：用户贴出两张关键帧质问"人物反正坐、又坐到驾驶员位置"，并要求"优化工作流，生图效率太低、超级慢"。**TASK-033（补人/串味根治）**：取证定位——关键帧走 Qwen-Edit 图生图通道，每段固定传 3 张参考图（两张角色卡 + 场景卡，`_segment_reference_cards` 取满 3 张），而图生图模型"给谁的脸就把谁摆进画面"→ 单人构图里补出第二个人、两张卡特征互串（实测坐着的乘客腰上挂了司机的旧工牌、背景多出人影）。修复：`SubjectPlacement.in_frame` 结构化字段（第 0 帧是否可见，分镜 Agent 产出）+ 参考卡只传画面内角色（≤2）+ 场景卡、道具卡不再传 + 关键帧提示词锁死画面人数并对画面外角色写正向状态句。**TASK-034（并发提速）**：数据定位——单张图平台侧固定 ~200s，而"等待"全是自己串行造成（job 等待 2s/248s/463s/673s…累加）；修复：额度占满（TASK_QUEUE_MAXED）不消耗重试预算（防并发批量提交打成"重试用尽"全灭）、`running_video_jobs` 过滤 provider_task_id（防并发池重复执行同一视频=双倍花钱）、启动恢复改为池启动前跑一次（`--no-recover`/`--recover-only`，否则新 Worker 会打断他人在途任务——实测白费一张图）、SQLite busy_timeout=30s、新增 `scripts/run_workers.py` 池启动器。线上：3 Worker 池下同时 2 张图在跑（即 RunningHub 账号并发额度约 2），无额度拒绝。详见 tasks/TASK-033-034-keyframe-reference-and-concurrency.md
- 2026-09-16：**TASK-032 提示词管线根治（重影 + 位置错误，用户反馈驱动的通用修复）**：用户反馈"每个视频经常出现重影，一个人镜头出现两个一样的人；人物在道具中的位置很奇怪，应该是开车却坐在后排乘客位"，并要求彻底解决根源而非针对某个项目调整。**诊断（实证到行号）**：①重影根因=h3_compiler 把同一角色外貌锚点全文在 `<Picture N>` 与 `<Subject N>` 写两遍 + 锚点带"直视镜头"定妆卡摆拍词泄漏进剧情镜头 + 空镜策略把"空无一人"写进场景 visual_anchor 后被嵌进有人镜头（图文矛盾时模型复制或清空人物）+ 只给说话人建 Subject（不开口的在场角色成官方明令禁止的"没名字的那个人"）；实证帧 S03G03 三个陈默并排、S01G02 车外两个风衣男（剧本只有一人）。②位置错误根因=compile_keyframe_prompt 只锁外貌、无任何空间绑定（座位全靠 LLM 自由散文），视频接力锚点"frame 0 必须与关键帧完全一致"强制继承错误座位（S02G02 关键帧阶段就把陆峥画进副驾打电话），store 提示词缓存又挡住编译器升级。**修复（通用机制，不限定载具/座位）**：`Segment.subject_placements` 结构化开场位置状态（校验器强制在场角色逐人给出：座位/站位 + 朝向 + 控制关系，名字须对应在场角色资产、placement 必须英文，失败进反馈重试闭环）+ 编译器锚点唯一化（`<Picture N>` 只声明参考图角色 "identity only, not an additional on-scene person"）+ 在场角色全建 `<Subject N>`（含不开口者，说话人追加 (Sx)，编号独立成序）+ 角色/场景/道具锚点确定性消毒（卡片摆拍词、空镜状态词、白底棚拍取景词，存量编译期兜底不改 DB）+ `opening placement` 注入 Subject 与关键帧逐人锁（"each named character appears exactly once, in the stated position"）+ 关键帧回退描述剥台词口型 + produce/regenerate 一律实时重编译（存储文本降级为缓存，编译器升级对存量项目即时生效）+ AssetAgent anchor 卫生规则 + 分镜规则 C6（位置状态显式化）/C7（关键帧纯静态）。+ 生成类动作忙碌态重入（生成 Job 失败不再永久卡死项目，重复派发由任务表护栏拦住）。**验证**：新增 test_h3_compiler.py 15 项 + 状态机/护栏测试，后端 188 项全绿（多轮连跑）；顺带修掉 FakeLlm 随机挑资产导致的闪失败；6 个存量项目 69 段干跑抽查——37 段去重角色锚点、18 段剥"直视镜头"、25 段剥"空无一人/空置"、22 段剥道具"白底/崭新未使用"、61 段补全在场角色 Subject、0 段超字符契约（对旧数据立即生效，未写库未生成媒体）；**线上重启 + 实战**：Worker/API 重启后重派被卡死的「超神奶爸」分镜任务首次即成功（47s），v2 分镜 12/12 段带 subject_placements，编译产物锚点唯一、位置逐人注入。详见 tasks/TASK-032-prompt-identity-placement.md
- 2026-09-16：**测试项目全链路实战验收通过（p-0a7e731af817「末班车复仇」，剧本→资产→分镜→关键帧→视频→成片一次贯通）**：成片 films/film_v1_sub.mp4（76s / 480×864 / AAC 音轨 / 字幕与台词逐字对应，11 段 3 场景 3 角色）。①Edit 参考版关键帧首次全量实战：11 段锚点帧人物与资产卡同脸、跨段零漂移（对比旧文生图版"三张三个脸"），48 币/张；②逐段抽帧质检 11/11：动作戏（扑抢/按压车门/戴手铐）、道具节拍（工牌挂腰/录音红波形/散落现金/前方断头路中文警示牌/逮捕令）全部命中；唯一缺陷 S01G03 第 0 秒资产卡泄漏（白底定妆照闪现 1s）→ regenerate_segment 换种子重掷后清零；S03G03 锚点首版群像复制 → 加"no people"空镜锚点策略修复。③运营级修复：TASK_QUEUE_MAXED 长退避（engine `queue_full_retry_delay_sec=120` + 测试 ×2——短指数退避实测被视频长任务烧光 3 次重试、13 张资产图全灭后靠 DB 重置恢复）；测试 +2 后后端 167 项全绿。④总消耗约 4860 币（image 43 次/905 + video 47 次/3955，含 EP001 并行会话与重复任务）。遗留待办：produce_video REF_MISSING 中断时已入队任务不清理 → 重跑重复生成（本次多花约 300 币，建议按分镜版本幂等跳过）；EP001 旧文生图关键帧未替换；S01G03 类资产卡泄漏可在锚点描述里预防性加"no white background studio card"负向语义。
- 2026-09-16：**EP001 全链重跑完成（节拍流+画幅+关键帧六阶段全链验证）**：新项目 p-5dba0072e423「代驾背锅夜（EP001 节拍流验证）」9:16 竖屏，COMPOSED——成片 films/film_v1.mp4 + film_v1_sub.mp4 **480×864 竖版 152.3s 带音轨字幕**（17 段合成），总耗 2485 RH 币。验证结论：①P0 画幅修复真实生效（关键帧+成片全竖版）；②剧本节拍流落地（4 场 96 拍，action/sfx/on_screen_text 齐备，台词全带语气）；③分镜 18 段 Σ152s、节拍认领 33/33、动作密度 67-155 词/镜、subject 绑定+retention 分化全在提示词中；④抽帧目检：陆峥脸/代驾服与定妆卡高度一致、on_screen_text（代驾平台订单）画进头盔、右手掌划伤锚点呈现、风衣男/李警官到位。**过程中实测触发并修复五个管道缺陷**：a.剧本台词无总量预算→与分镜逐字对账死锁（822 字 vs 45s），ScriptAgent 加「总字数≤目标×3、单句≤50 字」+warning 护栏；b.节拍动作段使单卷分镜输出膨胀 3 倍（25853 字符 json_invalid）→run_storyboard 改**分场景调用**（每场景一次 LLM 调用+场景间 continuity 链接）；c.自动加长 12s 与校验器 >8s 硬错误互斥（TASK-029 遗留）→统一放宽到 H3 上限 15s；d.节拍认领 LLM 长清单偶漏（16/17）→确定性兜底：漏拍挂末镜并附节拍原文重编译；e.超载台词段（76 字）→确定性二分拆段（台词均分+temp key+重编号+continuity 修正）。**A/B 结论**（陆峥同 seed 半身 vs 全身）：全身版裤子/外套下摆证据完整、脸部仍清晰，建议默认模板改全身立绘（与 TASK-030 B 方案三视图卡并列候选，待用户拍板）。**校准项**：台词预算 GLM 执行不严（383 字 vs 180），成片被动放长到 152s；资产 21 张偏多；LLM_MAX_TOKENS 8192→20000。运行期间与 TASK-030/031 会话同仓并行开发，关键帧阶段已并入本跑验证（17 帧生成+确认+尾帧回退）。驱动脚本：scripts/run_ep001.py + run_ep001_continue.py。
- 2026-09-16：**关键帧一致性根治：Edit 参考版图生图工作流（代码就绪，等工作流 ID）**：针对"纯文生图关键帧锁不住脸"（EP001 实测：三张关键帧三张脸、细节物件幻觉、画内文字污染）——①新工作流 `workerflow/QWEN图生图（Edit参考版单段）_api.json`：Qwen-Image-Edit-2509（UNET `qwen_image_edit_2509_fp8_e4m3fn`，CLIP/VAE 与文生图版共用）+ `TextEncodeQwenImageEditPlus` 三参考图槽位（节点 10/11/12，经 FluxKontextImageScale 预缩）+ ModelSamplingAuraFlow(shift 3) + CFGNorm，20 步/cfg 4；上传步骤与节点契约见 `workerflow/QWEN图生图（Edit参考版单段）-使用说明.md`；②适配器 env 开关：`RUNNINGHUB_WORKFLOW_IMAGE_EDIT` 配置后，关键帧任务自动携带在场资产卡已批准主图（`_segment_reference_cards`：角色优先其次场景 ≤3 张）→ 适配器上传参考图填 10/11/12、切 Edit 工作流、正向/负向写节点 6/7 的 `prompt` 字段，不足 3 张复用第一张；留空 = 行为与纯文生图完全一致；③测试 +2（edit 分支节点组装/回退路径），后端 165 项全绿。**实测通过（2026-09-16）**：工作流 2099913140015292417 用户已发布，.env 已配置、探针脚本直连适配器抽 S01G01 关键帧成功（48 币/237s）——脸部/发型/服装与陆峥资产卡逐项一致（对比旧文生图版的三张三个脸），无文字污染无物件幻觉，唯一小出入：电动车画成电摩样式（提示词道具细节，非身份问题）。待办：EP001 存量 17 张文生图版关键帧不自动替换，建议只对主角近景段逐段重抽（48 币/张）。
- 2026-09-15：**TASK-031 关键帧阶段（豆包 doubao-creative-drama 流程结构层落地，六阶段流水线）**：新增「分镜确认 → 关键帧 → 视频」第四确认门。①领域层：Stage.FRAME + FRAME_DRAFTING/READY/APPROVED 三态 + JobType.FRAME_GEN + SegmentFrameImage 实体（迁移 0004 已应用）+ Segment.keyframe_description（英文开场锚点静态描述，分镜 Agent 产出，存量空串兼容）；produce_video 前置改为 FRAME_APPROVED；分镜重生成失效回退覆盖 FRAME 态（FRAME_DRAFTING 忙碌态不放开）。②提示词：compile_keyframe_prompt 确定性编译关键帧生图提示词（风格行+锚点描述+在场资产 visual_anchor 锁+画幅；QWEN 纯文生图无图输入，身份一致性仍由资产卡承担）；compile_h3_prompt 增 opening_frame 参数——非连续段补关键帧时重编译出 Picture 1 槽位防编号错位；锚定句/retention 措辞中性化（尾帧/关键帧两来源通用）。③解析：_resolve_references 关键帧优先——已批准关键帧占 Picture 1、continuity_prev 清空（解除 DEPENDENCY_BLOCKED 对前段成片的依赖）、>9 截断保关键帧槽位；快照新增 opening_frame_source=keyframe/tail_frame。④门禁：approve_keyframes 默认逐段 ≥1 批准帧，显式 allow_tail_fallback 才允许缺帧段回退尾帧接力（不静默降级）。⑤UI：六 Tab（新增关键帧 FramesTab：锚点描述可编辑/帧图批准/上传/seed 重抽/缺帧回退勾选）+ StageNav 六阶段 + 剧作基调创建选择器。⑥E2E 脚本插入关键帧阶段。后端 163 项/前端 16 项全绿；真实 RunningHub 关键帧生图验证待用户择机执行（每段约 21 币）。
- 2026-09-15：**TASK-030 双轨剧作法 + 分镜细节规则（豆包 doubao-creative-drama 方法论内化第一步）**：①`ProjectParams.dramatic_tone`（hook 默认/three_act，`DramaticTone` 枚举）+ `update_params` 开放 + 创建项目表单基调选择器；②ScriptAgent 按基调注入结构法则——hook：黄金开局/高频反转/压迫具体化/反派合理性动机+片内惩罚铁律/台词千人千面；three_act：单一核心事件/三幕 25-50-25 配比/允许留白/对立面意象化/台词克制；两轨通用情绪外化+动作客观可拍；③StoryboardAgent 追加 `STORYBOARD_CRAFT_RULES`（POV 归属+镜内禁该角色正脸、禁群体量词逐个点名、方向性物体六要素公式、情绪外化、光影意图）；④AssetAgent 增 `CHARACTER_CARD_B_PROMPT`（豆包角色卡口径：左 1/3 正脸特写+右 2/3 三视图）作为角色卡 A/B 实验的 B 方案常量，默认仍官方半身定妆像。后端 155 项/前端 14 项全绿；存量项目 dramatic_tone 缺省 hook 自动兼容。
- 2026-09-15：**提示词与画幅优化轮（EP001 评估落地）**：对照官方方言+库里真实提示词逐项审后修复——①P0 画幅硬伤：工作流模板节点硬编码 864×480 横版、资产卡写死 16:9，9:16 项目（EP001 代驾）会出横版成片；现在 produce/regenerate 快照携带 ratio → generate → dims_for_ratio 换算写入 timeline 顶层+output 节点，资产卡尺寸 size_for_asset_card(ratio) 跟画幅、场景卡竖版纵向三面板、提示词画幅词随 ratio 生成；②P1 说话人绑定：有角色资产背书的说话人在 subject_definitions 生成官方句式 `<Subject N> is … (SN) in <Picture M>`，detailed_description 台词改由 `<Subject N> (SN) says` 引出（ID 绑脸防串音，旁白等无背书者保留原名），retention_analysis 按类型分化（角色只锁身份/场景只锁地理/道具锁形状标记/尾帧只锁开场构图）；③P2 生图：道具一卡一件（成对道具拆卡，防特写参考身份稀释）、角色卡兜底模板删掉写死 realistic style；测试 145 项全绿。待办：角色卡半身 vs 全身 A/B 实验（烧生图币，等用户拍板）；EP001 项目重跑全链验证 beat 流+本轮效果。
- 2026-09-15：**剧本节拍流改造（画面描述根治轮）**：用户对照 EP001 文学剧本指出平台剧本"只有对白没画面"，根因=ScriptAgent schema 无动作字段+提示词禁写动作+入库剥离括号指示，下游分镜只能从一句 summary 脑补、视频提示词密度必然不足。修复：①`Scene.beats` 节拍流（action/dialogue/sfx/on_screen_text/transition 五类，EP001 口径提示词重写，场景头三要素「内/外·地点·日夜」），dialogues 改由 dialogue 节拍派生（括号语气归位 `tone` 不再丢弃）；②分镜 shots 增 `beat_refs`（认领节拍，action/on_screen_text 100% 认领否则确定性校验重试）与 `action`（英文动作时间线段，≤5s 镜 60-120 词/6-8s 镜 100-160 词，直供 detailed_description 密度）；③`Segment.soundscape/music` + style 行来自项目参数——h3_compiler 三处硬编码（"Rain against the glass"音效/氛围垫乐/恒定夜景电影感）全部改为数据注入+中性兜底；④旧剧本 beats=[] 向后兼容，`run_script`/`run_storyboard` 双入口做 beats↔dialogues 互推导降级；⑤前端剧本 Tab 按节拍流渲染（台词带语气）、分镜时间线展示英文动作段。新增 test_beat_flow.py 14 项，后端 139 项全绿、前端 14 项全绿。存量项目需重新生成剧本+分镜才有节拍流。
- 2026-09-15：**视频工作流切换满血版（用户导入发布，workflow 2099856521889935362）**：新工作流去掉了初版的 Refine 二段精修链，导演节点 12 / 成片输出节点 7 契约不变，timeline 仍为 version 5 同构（多 videoWorkspaces 与未接的 t2v 批处理工作区）；`video.py` 模板解析改为按 `.env` 工作流 ID 匹配 `workerflow/MiniMaxH3Director_ref2va_<ID>_api.json`（无同名文件回退初版模板），`.env` 已切新 ID，`build_timeline` 对新模板组装冒烟通过、6 项视频适配器测试全绿。注意：新工作流内嵌出图分支（节点 28–53，anima+16 LoRA→SaveImagesToZip）每次任务都会连带执行，产物 zip 不影响视频挑图；若真实跑批显存不足或平台缺模型导致失败，在 RunningHub 删节点 28–53 重新发布。验收点与回滚步骤见 workerflow/满血版导演台工作流-使用说明.md
- 2026-09-15：**口播截断根治（TASK-029 终版 film_v6 交付）**：用户反馈"说话没结束被截断"。修复：①口播校验 5→4 字/秒；②自动加长上限 8→12s 且同步均布重算切点；③删除缩放兜底（缩时长必超口播预算，正是截断元凶）；④Σ总时长降为警告（台词完整 > 时长精准）。配套修复 clips 未随分镜版本迭代迁移（尾帧接力断链根因）。**最终交付 film_v6_sub.mp4（58.32s）**：台词段全部 ≤4 字/秒不掐断，film 为 v14 storyboard 渲染+电影化合成齐备
- 2026-09-15：**referenceVideo 视频续接实验（TASK-029 追加）**：接入导演台 referenceVideo 槽位（前段成片上传+continuityFromPrev），S01G02 A/B 实测语义偏"内容参考"而非帧 0 续接（生成从外景重启、道具提前、台词丢失）→ 该段回退 v14 渲染，代码保留默认关闭（settings.h3_reference_video）；同轮修复 .env 三件套错位（GLM key/base+deepseek 模型名 → 400）。**最终交付 film_v5_sub.mp4（54.29s，9 段 v14 渲染+电影化合成）**
- 2026-09-15：**GLM-4.6 切换成功 + film_v4 交付（TASK-029 终）**：用户提供 GLM key 后切 glm-4.6（JSON mode 原生支持、结构完整），配合分场景调用（输出缩至 1/3 不再截断）成功生成 v14 分镜（9 段 55s 全双镜+强锚定+服装锁）。9 段全量重出+逐段验收后合成为 **film_v4_sub.mp4（54.29s）**：定妆卡级服装一致性（围裙/工牌全片在位）、末帧"小满工牌+月牙怀表"特写收尾。glm-5.3-flash 输出偷懒弃用；deepseek-reasoner 不支持 JSON mode（适配器已加绕行）且 CoT 吃 max_tokens 需 32K 预算。**新增 h3_compiler.py 六段提示词确定性编译器**（LLM 只产结构化数据，提示词代码编译——V3 哲学落地）+ 章节顺序规范化 + base→reference 升级 + 台词模糊还原 + LLM_TIMEOUT_SEC 配置化
- 2026-09-14：**一致性根治轮（代码就绪，待 DeepSeek 充值续跑）**：跨段脸/服装/发态漂移根因=四视图拼贴卡弱身份锚+卡与尾帧证据冲突+链式累积。修复：①角色卡改单视图正面定妆照（模板+兜底，两张已出）；②切段 8s 双镜（段数 9→6 切点减半）；③segment_key 确定性规范化（`_normalize_segment_keys`，index 场景内序号+key 同步+continuity 引用修正）；④解析期 key 强校验移除（下沉 validate_segment）；⑤SEGMENT_KEY_RE 允许 1-2 位场景号；⑥Job 原子认领（防多 Worker 双跑）；⑦shot 描述英文短语化（防 8192 截断）。**发现 scripts/watchdog.sh 三层守护自动拉起旧 Worker**——开发期需停，生产行保活可用。阻塞：DeepSeek 402 余额不足，续跑步骤见 tasks/TASK-029
- 2026-09-14：**V2/V3 四件套移植 + 9 段全量重出（TASK-029 追加）**：针对"5s 单镜头硬切感"反馈，研究 short-dreamV3 实现后移植：①连续段 Picture 1 强锚定句（LLM 漏写由 `_ensure_relay_anchor` 确定性注入）；②相邻段景别交替/位置感知/固定陈设一致（规则 8）；③段尾 end state 衔接；④口播预算确定性校验（字数>时长×5 报错）。配套：提示词密度按时长分级防输出超限、chat_json 失败明细落 stderr。9 段重产后服装一致性显著改善（围裙/工牌全片在位）；S03G09 首抽整段画风坍塌成动漫、重抽恢复。成片 film_v3_sub.mp4 44.37s。约 470 币
- 2026-09-14：**拼接感诊断 + 电影化合成上线（TASK-029 追加）**：根因=尾帧仅语义参考（H3 first_frame/reference 互斥）+referenceVideo 未接+硬拼合成+服装描述泛化。新增 `FFmpegService.compose_film`（统一调色+0.25s xfade 交叉淡化+音轨 acrossfade+连续雨声音床），字幕按新时间轴对齐；compose 重试状态机修复（video_ready→composing）；H3 规则新增服装逐字锁。成片 film_v2 44.34s 对比版产出。结构性优化（reference_video 续接/工作流多段批处理/first_frame 链）见 tasks/TASK-029
- 2026-09-14：**TASK-029 端到端质量测试与自我修复迭代完成（雨夜关东煮 p-e6fd455a9702）**：9 段×5s 成片 46.16s 带字幕（film_v1_sub.mp4），逐段人工级验收（含末秒崩坏抽检），全流程 1721 币。12 项问题闭环：①AssetAgent 资产分类硬规则+微缩模型词禁令+道具消毒正则扩展；②切段策略确定性校验（段数/Σ时长/单段≤8s/shots≤2）；③剧本台词↔dialogue_refs 逐字对账；④`<d>` 块反向对账（防提前读其他段台词）；⑤LLM_MAX_TOKENS=8192 防长 JSON 截断；⑥build_timeline 同步 segment 帧数（修复任何时长都出 10s 的严重缺陷）；⑦产物下载全解码校验+重下；⑧cmd_edit_segment 两 bug；⑨produce_video 允许 VIDEO_PRODUCING 重入；⑩清杀两个遗留旧代码 Worker（任务双跑根因）。逐段验收方法论与遗留建议见 tasks/TASK-029-e2e-quality-test.md
- 2026-09-14：**TASK-028 道具卡独占画面 + 排版稳定性**：①道具模板改纯正向产品图描述（isolated/unworn，删除 "no hands" 反向引入句），新增 IMAGE_NEGATIVE_PROP（hands/person/background/clutter）与 sanitize_prop_prompt（双入口强制）；外卖服+外卖箱实测纯白底无手无人。②角色模板写死"恰好四个视图"计数。排版残差说明：文生图对面板数控制天然 probabilistic，坏卡处理=删+换 seed 重出。
- 2026-09-14：**v2 单段工作流已上线实测**：workflow 2099181966271795202（用户导入发布），.env 已切换；林宇同 seed 实测——GPU 计算约 60-70s（v1 双段约 6-7 分钟，提速 5 倍+），四视图构图/一致性/负向词全部正常，输出 1920×1080（1.26MB vs v1 4K 6.8MB）；本轮总耗时 381s 中约 310s 为 RunningHub 队列排队（平台侧，与工作流无关）。
- 2026-09-14：**v2 单段工作流（速度优化）**：稳定版实测约 7 分钟/张过慢，根因=4K 高清修复链（4x 放大→缩回→二次 20 步采样）占大半耗时且超出卡片用途；v2 去掉整条放大链、16 步单段直出 1920×1080（预计 1/3 耗时），节点契约不变，等用户导入拿 ID 切换。
- 2026-09-14：**稳定版出图工作流已上线并实测通过**：用户在 RunningHub 导入发布（workflow 2099172891819597826），.env 已切 RUNNINGHUB_WORKFLOW_IMAGE + IMAGE_NEGATIVE_NODE=7；「林宇」实测（seed=20260914，约 7 分钟）——四视角一行严格一致 + 书法名字 + 签名道具多角度，正式设定卡水准；负向通道（质量+场景人形）随工作流生效。
- 2026-09-13：**TASK-027 稳定版出图工作流**：workerflow/QWEN文生图（稳定版）_api.json——去 Lightning LoRA、20步 cfg 2.5（Qwen 官方原生口径）、节点 7 改真实负向 CLIPTextEncode（内置质量负向），节点 5/6/8 编号与极速版一致（适配器契约不变）、保留二阶段放大链；代码侧负向词按资产类型自动组装（场景版含人形负向，textnorm 常量）并经 payload.negative → ImageProvider(negative) → 节点字段全链透传；IMAGE_NEGATIVE_NODE 环境变量做开关（留空=极速版兼容）。启用步骤见 workerflow/稳定版出图工作流-使用说明.md（RunningHub 导入 JSON 拿新 ID → .env 两行）。
- 2026-09-13：**TASK-026 出图稳定性**：①seed 透传全链（generate_asset_image payload.seed → ImageProvider.generate，缺省随机不变；固定 seed+同提示词=可复现）；②资产卡模板收紧排版约束（角色视图横排一行且严格一致/场景三面板等宽并排一行），降低多面板布局随机性；③维护脚本保留规则修正——生成中/失败的行不再顶掉已落盘的老图（landed 优先）。稳定性根因说明：随机 seed + Lightning 6步 cfg=1 无负向通道 + 多面板提示词本身高方差。
- 2026-09-13：**TASK-025 分镜确认后可重生成**：generate_storyboard 放开 STORYBOARD_APPROVED/VIDEO_READY/COMPOSED 三态（FR-016 回退 STORYBOARD_READY 再入 DRAFTING，忙碌态不放开），侧栏「重新生成分镜」按钮同步——解决老项目分镜「编号错位」徽标无处修复的问题；旧分镜版本与成片保留。
- 2026-09-13：**场景模板二次修正（实测驱动）**：①去掉俯视平面图面板（用户不要草图；且"no floor plan"否定句又会把草图引进来，模板中已无任何否定句）；②新增设施词中性替换（保安亭→空置玻璃亭、门禁闸机→出入闸栏、guard booth→empty glass kiosk 等加入 sanitize_scene_prompt）——"保安亭+门禁"的语义先验会稳定画 出保安，替换后「高档小区入口」实测三视角零人物成功；
- 2026-09-13：**场景图无人确定性护栏**：textnorm 新增 sanitize_scene_prompt（剥 people/silhouette/mannequin/人物/剪影等人形词，含 "no X" 对），在 generate_asset_image 与 asset_extract 两个入口对场景类强制执行；资产 Agent 场景规则同步「禁写任何人形词」；存量场景图逐张目检（3 张均无人，高档小区入口旧图已被用户手动删除）。
- 2026-09-13：**TASK-024 场景卡多视角（防物体漂移）**：场景卡从单视角空镜升级为单图多视角（主视角+反打+俯视平面布局，地标跨视角一致）；两次实测教训——负向提示词在 Lightning cfg=1 工作流里完全无效且反向引入人形（"no people"→更多人），改为纯正向描述（deserted and empty+只写在场物）后彻底无人；「深夜渡口」实测 4 视角无人卡成功（3 次生成约 70 币）。
- 2026-09-13：**TASK-023 资产图删除**：新增 delete_asset_image 命令（删库行、磁盘文件留底可恢复；删光后资产门按缺批准图正常拦截），资产卡图上加「删」按钮带确认——生成多版本后可手动清理旧图。
- 2026-09-13：**TASK-022 图片放大预览 + 出图风格确认**：①新增 ImageLightbox（点击遮罩/Esc 关闭），资产图与分镜参考图（折叠行小图+展开大图）点击放大，小图点击不触发折叠；②新增 update_params 命令（style），资产 Tab 顶部风格选择器（6 预设：3D 皮克斯/写实电影/国漫/日漫/水墨/赛博朋克 + 自定义），cmd_generate_asset_image 生成时自动把项目风格拼入提示词（已含则跳过）——改风格无需重写提示词模板。
- 2026-09-13：**多视角卡实测通过**：规范化脚本升级为维护工具（提示词重写为官方卡模板 + 同标签保留最新图），16 条存量提示词全部重写；林宇真实生成验证——单图含特写/正面/3/4/背面+名字标注，官方角色卡结构一次成型（约 23 币），旧图自动清理转正。环境发现：本机系统代理会拦截 python urllib 对 127.0.0.1 的 POST（502），脚本访问本地 API 需 ProxyHandler({}) 直连。
- 2026-09-13：**存量资产卡规范化落地**：新增 scripts/normalize_asset_cards.py（dry-run/--apply），对两个存量项目裁剪 7 个多视图 image_plan、删除 9 张拆分图（磁盘文件留底），16/16 资产单图；handle_asset_extract 改为整体替换（delete_assets_for_project，重新抽取不再叠加旧卡，回归测试覆盖）。
- 2026-09-13：**TASK-021 视频提示词动作时间线对齐官方 h3-prompt-writing**：对照官方 ref-en（detailed_description 350-500 词、每镜写时间窗内动作渐进「开始→中途→结束」、运镜类型+幅度+速度、镜头内音效、风格定调句在 [Shot 1] 之前）重写 H3_RULES_DIGEST 规则 3；实测旧提示词仅 115 词状态快照，重新生成分镜即按新密度产出；UI 动作时间线（TASK-019）与提示词时间线互补。
- 2026-09-13：**TASK-020 资产卡对齐官方 H3 规范**：对照官方 MiniMax-AI/MiniMax-H3《3d-animation-short-generator》（角色卡=单图含主 3/4+正侧背三视图+表情+细节+道具标注，场景卡=无人空镜+光态+地标，一律 16:9）重写 AssetAgent 提示词；LLM 违规拆分的「面部特写/半身像」确定性裁剪（这类图从不进 H3 参考，只耗生图币）；出图固定 1920×1080（size_for_asset_card）。存量项目的多视图资产不受影响，重新抽取资产即按新规范。
- 2026-09-13：**TASK-019 成片字幕 + 台词污染治理 + 动作时间线**：①compose 后自动烧字幕（SRT 按 shots 切点+Clip 实测时长生成；films 加 subtitle_path 列，迁移 0003 已应用；FilmTab 默认播字幕版）；状态机补 COMPOSED→compose 重新合成——「渡口夜行」实测产出 film_v2_sub.mp4（70.9s，抽帧确认字幕清晰）。②「（扑通跪下）」被读成台词根因=剧本台词含括号舞台指示被逐字进 <d>：ScriptAgent 禁令 + textnorm 确定性剥离（含存量剧本入分镜前清洗）+ validate_h3_prompt 拦 <d> 含括号。③分镜 Tab 每段动作时间线（比例时间条+逐镜起止秒）。④参考图未显示=API 旧进程未重启（已重启并验证），折叠行补内联缩略图。详见 tasks/TASK-019-subtitles-dialogue-timeline.md
- 2026-09-13：**TASK-018 用户实测反馈三连修（剧本可改 / 资产可增改+分镜参考图可视化 / 视频一致性）**：①一致性根因=连续段 `<Picture N>` 编号整体错位（Agent 不知道尾帧占 `<Picture 1>`），补 H3_RULES_DIGEST 规则 6/7 + validate_segment 编号硬校验 + 截断保序 + 主设定/空镜确定性取图；②新增 create_asset/edit_asset 命令（含 FR-016 回退）+ 资产 Tab 新增/编辑/自定义提示词生成；③分镜路由解析实际参考图（含尾帧），分镜 Tab 缩略图条 + 编号错位徽标；④剧本已确认版本可编辑（结构化编辑器）+ 修复 CREATED 手写首稿 STATE_ILLEGAL 卡死；规格同步（API_SPEC 命令表 / AI_SPEC 编号硬校验），详见 tasks/TASK-018-user-feedback-fixes.md
- 2026-09-13：**TASK-017 真实端到端完成（M4）**：「渡口夜行」想法 → 3 场景 9 资产 16 图 → 7 段视频（357 币，31 分钟）→ 成片 70.9s（40MB，含音轨，Σ误差 0.03s），总耗 607 RH 币；分镜确定性校验首败后退避重试成功（护栏按设计工作）
- 2026-09-13：**TASK-008-012 用例层 + TASK-013-016 工作台 UI 完成**：17 个领域命令 REST、三 Agent、Worker 真实 handlers、三栏工作台五 Tab、设置页；后端 100 测试 + 前端 14 测试全绿
- 2026-09-13：TASK-007 VideoProvider + FFmpegService 落地并真实冒烟通过（阶段 3 完成）；进入阶段 4 用例层

## 下一个任务
1. TASK-005（LlmProvider：OpenAI 兼容 chat + JSON mode 结构化输出 + 重试）
2. TASK-001（Spike 执行，等 API key；结果回写 AI_SPEC 附录 A）

## 智能体交接备注
- 规格包是唯一事实来源；动手前先读 **AGENTS.md**、AI_RULES.md 与本文件
- **改完 `server/`、`scripts/` 下源码必须重启 API 与 Worker**（进程只在启动时加载代码）——
  违反会按旧规则生成并照常扣币（2026-09-16 实测白花 89 币）；步骤与自检见 AGENTS.md 第 1 节
- H3 请求组装的契约红线在 AI_SPEC 附录 A，改动须评审
- 领域层入口：server/domain/__init__.py 导出 41 个名字；跨实体校验在 validation.py，段内结构不变量在 entities.py 模型构造时强制
- 队列链路：WorkerEngine（server/worker/engine.py）只管 Job 生命周期；真实 handler 未接前由 server/adapters/fake.py 保持全链可跑，TASK-005-007 逐个替换
- 本地 H3 提示词方法论参照：C:\Users\Admin\.agents\skills\dj-novel-storyboard\references\h3-prompt.md（已内化进 AI_SPEC，不作为运行时依赖）
